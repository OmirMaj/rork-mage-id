# operations (observability, scheduled jobs, notification/email pipeline, secrets/env, web hosting, recoverability) — final-push audit — 2026-09-03

Repo is read-only; production access was SELECT-only SQL, `list_edge_functions`, `get_edge_function` (notify), `get_project`, `get_organization`, `query_logs` (24 h window — Free plan retains 1 day), `get_advisors`. No DDL/DML, no deploys. Live rows are quoted verbatim where a claim depends on them.

## Scope covered

Files read end to end: `CLAUDE.md`, `docs/START-HERE.md` (banner + 120 lines), `DEPLOY-RUNBOOK.md`, `DEPLOY-VERIFIED-2026-09-02.md`, `LAUNCH.md`, `LAUNCH-CHECKLIST.md`, `REPAIR-HISTORY.sh`, `docs/deploy/2026-08-19-release-build-prep.md`, `docs/deploy/2026-08-19-session-two-recap.md`, `app.json`, `eas.json`, `netlify.toml`, `marketing/netlify.toml`, `marketing/_redirects`, `marketing/robots.txt`, `public/robots.txt`, `public/manifest.webmanifest`, `.netlify/netlify.toml`, `marketing/.netlify/netlify.toml`, `ios/MAGEID/MAGEID.entitlements`, `ios/sentry.properties`, `.gitignore`.
Edge functions: `notify/index.ts` (all 950 lines, repo AND deployed copy via `get_edge_function`), `send-email/index.ts`, `unsubscribe/index.ts`, `_shared/auth.ts`, `_shared/cronAuth.ts`, `_shared/verifyUser.ts`, `_shared/email.ts` (footer/token sections), `morning-digest/index.ts` (130-200, 360-481), `homeowner-weekly-digest/index.ts` (515-571), `invoice-dunning/index.ts` (471-581 markers), plus targeted greps over every function for `Deno.env.get`, cron auth, idempotency, rate limiting, receipts.
Migrations: `schedule_all_crons.sql`, `schedule_daily_digest.sql`, `20260523130000_cron_secret_guard.sql`, `20260526120200_qbo_reconciler_cron.sql`, `20260608130000_notify_nearby_cron_secret.sql`, `20260826180000_portal_link_expiry_cron.sql`, `notify_triggers.sql`, `grant_fire_notify_to_anon.sql`, `create_email_unsubscribes.sql`, `20260819160000_orphaned_tables_schema_backfill.sql` (head). `supabase/schema.sql` sections: `fire_notify` (4296-4330), `is_email_unsubscribed`, `notify_*_fn` (4664-4732), `public_bids_notify_nearby_fn` (5115-5140), `trg_notify_*` + `verify_cron_secret` (5571-5783), trigger declarations (3661-3703).
Client: `app/_layout.tsx` (1-175, 244-275, Sentry/PostHog/ErrorBoundary wiring), `components/ErrorBoundary.tsx`, `utils/posthog.ts`, `utils/analytics.ts`, `utils/notifications.ts`, `utils/notifyClient.ts`, `utils/localCacheKeys.ts`, `utils/offlineQueue.ts` (error-handling grep), `hooks/useNotificationFeed.ts`, `contexts/NotificationContext.tsx` (grep), `marketing/portal/index.html` (notify call sites 3600-3640, 6620-6660), `scripts/validate-outbox-contract.ts`.
Production queries: `cron.job`, `cron.job_run_details` (last 100 + per-job aggregates + since-flip counts), `net._http_response` (status distribution + last 70 bodies), `pg_settings` (cron.timezone, pg_net.ttl), `public.app_config`, `notification_outbox` (columns, counts by event/kind/status, last 40), `profiles` push-token stats, `rate_limit_counters`, `email_unsubscribes`, `storage.buckets` + `storage.objects` aggregates, trigger/function privilege introspection, source-table row counts after 2026-06-11, `cached_bids/cached_companies/cached_jobs/city_coords` freshness, orphaned-table RLS/policy counts, `current_setting('app.settings.service_role_key')`, `private.cron_auth` row count/length. `query_logs`: source counts, `function_edge_logs` grouped by path/status/UA (24 h), `function_logs` last 40 lines.
Live HTTP probes (read-only GET/HEAD): `https://app.mageid.app/` and `https://mageid.app/` headers, `/.well-known/apple-app-site-association` on both, `/.well-known/assetlinks.json`, `/support`, a nonexistent path on both origins, a hashed `_expo/static` chunk. Supabase docs fetched for plan limits (backups, edge limits, pricing).

## Findings (ranked; most severe first)

### F1 — P1 CONFIRMED — Three of the eight pg_cron jobs (morning-digest hourly, qbo-reconciler every 30 min, invoice-dunning daily) have been rejected at the Supabase gateway with HTTP 401 on every fire since late July, while `cron.job_run_details` records every one of those 2,898 fires as `succeeded`.
- Where: live `cron.job` rows jobid 7 (`morning-digest-hourly`, `5 * * * *`), 23 (`qbo-reconciler-every-30m`, `*/30 * * * *`), 15 (`invoice-dunning`, `0 15 * * *`); `list_edge_functions` → `morning-digest` v9 `verify_jwt: true` (deployed 2026-07-26 01:50 UTC), `qbo-reconciler` v2 `verify_jwt: true` (2026-07-26 05:25 UTC), `invoice-dunning` v5 `verify_jwt: true` (2026-08-03 19:42 UTC). The five jobs that work (`daily-digest`, `coi-expiry-watch-daily`, `geocode-bids`, `fetch-external-data-schedule`, `homeowner-weekly-digest-friday`) all target functions with `verify_jwt: false`. There is no `supabase/config.toml` in the repo (`ls supabase/` → `functions migrations schema.sql`), so every `supabase functions deploy <name>` without `--no-verify-jwt` resets the flag to true.
- Evidence (live):
  - `net._http_response` (pg_net keeps 6 h; `pg_net.ttl = 6 hours`): `{"status_code":401,"n":18}`, `{"status_code":200,"n":1}`, `{"status_code":null,"timed_out":true,"n":1}`. Every 401 body: `{"code":"UNAUTHORIZED_NO_AUTH_HEADER","message":"Missing authorization header"}` at :00/:30 (qbo) and :05 (morning-digest).
  - `function_edge_logs`, last 24 h, grouped: `/functions/v1/morning-digest 401 ×24 (pg_net/0.20.0)`, `/functions/v1/qbo-reconciler 401 ×48`, `/functions/v1/invoice-dunning 401 ×1 (15:00:01)`; `daily-digest 200`, `coi-expiry-watch 200`, `geocode-bids 200`, `fetch-external-data 200 ×4`. Log attribute on the 401s: `response.headers.sb_error_code = UNAUTHORIZED_NO_AUTH_HEADER` — that is the gateway, not function code (the functions' own rejections say `{"error":"unauthorized"}` / `"cron auth required"`).
  - `cron.job_run_details`: jobid 7 → 958 runs since 2026-07-26 02:00 UTC, jobid 23 → 1,909, jobid 15 → 31; rows with `status <> 'succeeded'` since 2026-07-26: **0**. `return_message` is always `1 row` — pg_cron only records that `net.http_post` was *queued*.
  - The job commands send only `Content-Type` + `x-cron-secret` (e.g. job 7: `headers := jsonb_build_object('Content-Type','application/json','x-cron-secret',(select secret from private.cron_auth limit 1))`). The one job that sends `Authorization` (job 4) builds it from `current_setting('app.settings.service_role_key', true)`, which is **NULL** in production (verified) → header value `Bearer ` — it only works because `fetch-external-data` is `verify_jwt:false`.
  - Deploy dates line up with commits that touched each function and were deployed by a session without the flag: `e0857add 2026-07-25 21:07 -0400 … morning-digest … (deploy deferred)` → deployed 2026-07-26 01:50 UTC; `aa8578e7 2026-07-26 01:14 -0400 fix(qbo-reconciler)` → 05:25 UTC; `dc6d584c 2026-08-03 02:31 -0400 feat(revenue)` → invoice-dunning 19:42 UTC.
- Failure scenario (what users see): the one `digest_enabled = true` profile (created 2026-03-25 — the founder) has received no morning briefing since 2026-07-26 and nobody noticed; the 1 `qbo_connections` row has had no reconciliation (no QBO payment pull-back, no stuck-invoice re-push) since 2026-07-26; the 3 invoices that are overdue and unpaid have had no dunning since 2026-08-03. `docs/START-HERE.md:39` says `invoice-dunning` "is STILL emailing clients a "FINAL NOTICE" … on every run" — it is not running at all. Flipping `verify_jwt` back **without** deploying the repo code would resume the deployed v5, which `DEPLOY-VERIFIED-2026-09-02.md:209-211` says still carries the retention FINAL-NOTICE bug.
- Fix: `supabase functions deploy morning-digest qbo-reconciler invoice-dunning --no-verify-jwt --project-ref nteoqhcswappxxjlpvap` (repo code + flag in one step). Then F3 (config.toml) so it cannot recur, and O1 (a cron-health check that reads `net._http_response`, not `cron.job_run_details`). Verify: next `:05` and `:30` rows in `net._http_response` are 200; `select count(*) from net._http_response where status_code = 401` = 0 after 6 h.
- Effort: S.

### F2 — P1 CONFIRMED — The database-trigger → `notify` path is dead for every GC-bound event it exists for (`portal_message`, `budget_proposal`, `co_approval`, `sub_invoice_submitted`, `sub_invoice_reviewed`): `fire_notify` authenticates with `app_config.notify_key`, which is the empty string, and the deployed `notify` treats an empty bearer as anonymous and refuses those events with HTTP 200.
- Where: `supabase/schema.sql:4306-4318` (`fire_notify`: `SELECT value INTO v_key … WHERE key = 'notify_key'` → headers `'Authorization', 'Bearer ' || COALESCE(v_key, '')`, `'apikey', COALESCE(v_key, '')`); `:4687-4707` (`notify_portal_message_fn` — POSTs to `/functions/v1/notify` with `'{"Content-Type": "application/json"}'` and **no** auth header); live triggers: `portal_messages → notify_portal_message_fn`, `change_order_approvals → trg_notify_co_approval`, `portal_budget_proposals → trg_notify_budget_proposal`, `sub_submitted_invoices → trg_notify_sub_invoice / trg_notify_sub_invoice_reviewed` (all `tgenabled = O`). Deployed `notify` (identical to `supabase/functions/notify/index.ts:236-243, 253-255, 928-936`): `ANON_ALLOWED_EVENTS = {contract_signed, selection_chosen, bid_question_asked, bid_question_answered, closeout_binder_sent}`; `if (isAnonCaller && !ANON_ALLOWED_EVENTS.has(event)) return { ok: false, reason: 'event_not_anon_allowed', event }`; `isPrivileged = isServiceRoleToken(bearer) || isServiceRoleToken(apikey) || (await verifyUser(req)) !== null`. `_shared/verifyUser.ts:44` → `if (!token || token.length < 20) return null`; `:83` → `!!token && …` → both false for `''`. The deployed notify has **0** occurrences of `x-cron-secret`/`isValidCron`.
- Evidence (live): `public.app_config` → `{"key":"notify_key","value":"","updated_at":"2026-04-26 18:33:37.062664+00"}` — the timestamp is the seeding migration's own (`20260426183337 notify_config_table`); it was never filled. `notification_outbox` last rows per event: `portal_message` 2026-05-23, `contract_signed` 2026-05-20 (an anon-allowlisted event — still works), `co_approval`/`sub_invoice_submitted`/`budget_proposal` only the 2026-04-30 seed rows. Source tables have **0** rows inserted since the notify v19 deploy (2026-06-12): `change_order_approvals 0, portal_budget_proposals 0, sub_submitted_invoices 0, portal_messages(client) 0` — so there is no live victim yet; the path is deterministic. The outer handler returns `jsonResponse({ success: true, result })` (HTTP 200), `fire_notify` swallows everything (`EXCEPTION WHEN OTHERS THEN RAISE NOTICE`), and pg_net is async — nothing anywhere records the drop.
- Failure scenario: homeowner sends a portal message / approves a change order / sub submits an invoice → the GC gets no push, no email, no inbox row (`hooks/useNotificationFeed.ts:56-70` reads the outbox — the inbox IS the outbox); the sub never learns their invoice was approved or paid. The portal shows "sent". Also: the live `portal_messages` trigger is the older `notify_portal_message_fn` (fires on GC-authored messages too, no `author_type = 'client'` filter) while the correct `trg_notify_portal_message` (`schema.sql:5667-5690`) exists unattached.
- Fix (smallest): (1) `notify/index.ts`: `import { isValidCron } from '../_shared/cronAuth.ts'` and add `|| (await isValidCron(req))` to `isPrivileged`; (2) replace `fire_notify`'s header block with `'x-cron-secret', (select secret from private.cron_auth limit 1)` (it is already `SECURITY DEFINER`; same pattern as `public_bids_notify_nearby_fn`); (3) `DROP TRIGGER notify_portal_message ON portal_messages; CREATE TRIGGER … EXECUTE FUNCTION trg_notify_portal_message()`; drop the three orphan `notify_*_fn`. Zero-code alternative that works today: `update app_config set value = '<service_role_key>' where key = 'notify_key'` (reads are locked — RLS on, no policies — but it puts the service key in a table; prefer the secret path). Verify with a test portal message → outbox row.
- Effort: S.

### F3 — P1 CONFIRMED — Both current runbooks instruct the operator to run `supabase functions deploy …` without `--no-verify-jwt`; executing `DEPLOY-VERIFIED-2026-09-02.md` step 4 as written would flip `homeowner-weekly-digest`, `notify`, `stripe-webhook`, `seal-document`, `mcp` and `portal-link-expiry-notice` to `verify_jwt: true` — the Friday homeowner digest, every DB-trigger and portal call into `notify`, Stripe webhooks (Stripe sends no Supabase JWT) and MCP clients would all start 401-ing exactly like F1.
- Where: `DEPLOY-VERIFIED-2026-09-02.md:205-206` (`supabase functions deploy invoice-dunning homeowner-weekly-digest delete-account --project-ref …`) and `:217-219` (`… mcp qbo-sync qbo-reconciler project-invite portal-link-expiry-notice notify seal-document stripe-webhook import-schedule award-rfp`); `DEPLOY-RUNBOOK.md:72`; `CLAUDE.md:109` ("deployed via `supabase functions deploy <name>`"); `docs/deploy/2026-07-07-production-handoff.md:72-74` — config.toml pinning "was NOT auto-applied … deferred deliberately"; `docs/security/2026-07-13-security-hardening-runbook.md:75` (a bare `for f in …; do supabase functions deploy "$f"; done`).
- Evidence: live `verify_jwt` today — `notify false, stripe-webhook false, seal-document false, mcp false, homeowner-weekly-digest false, portal-link-expiry-notice (not deployed; must be false — cron-only)`; the three F1 functions are the ones a session redeployed without the flag. Earlier plan docs did carry it (`docs/superpowers/plans/2026-05-18-d2h-unsubscribe-presend-guard.md:288`: `supabase functions deploy invoice-dunning homeowner-weekly-digest --no-verify-jwt …`) — the flag was lost when the runbooks were rewritten. Nothing in `ship-check` compares deployed `verify_jwt` to intent (`scripts/validate-edge-security.ts` reads source only).
- Failure scenario: founder follows the runbook → Stripe payments stop reconciling, Friday digests stop, the fix for F2 never works, MCP integrations break — and every one of those shows as `succeeded` in `cron.job_run_details` and as a 200 in Stripe's dashboard retry log only after Stripe gives up.
- Fix: commit `supabase/config.toml` with an explicit entry per function. `verify_jwt = false` for: `fetch-external-data, daily-digest, morning-digest, homeowner-weekly-digest, coi-expiry-watch, geocode-bids, invoice-dunning, qbo-reconciler, portal-link-expiry-notice, notify, notify-nearby-contractors, stripe-webhook, revenuecat-webhook, unsubscribe, auth-magic-link, seal-document, schedule-ical, financing-redirect, financing-callback, portal-mark-viewed, mcp, mcp-token, public-lead-intake, public-cost-index, widget-estimate, qbo-connect-callback` (matches today's live values); `true` for the rest. Rewrite the deploy lines in both runbooks and `CLAUDE.md`. Add a guard that fails ship-check when `list_edge_functions` verify_jwt ≠ config.toml (needs the MCP or `supabase functions list --project-ref`).
- Effort: S.

### F4 — P1 CONFIRMED — The production project is on the Supabase **Free** plan: no daily backups, no PITR available, 1-day log retention, 150 s edge wall-clock, auto-pause after 7 days of inactivity. There is no backup of any kind and no runbook mentions it.
- Where: `get_organization(niudaouqbomgqimilsij)` → `{"name":"mageid-production","plan":"free"}`; `docs/START-HERE.md`, `LAUNCH-CHECKLIST.md`, `DEPLOY-*.md` contain no backup/plan item.
- Evidence: Supabase docs (fetched 2026-09-03) — backups: "Free Plan: Not included … We recommend that free tier plan projects regularly export their data using the Supabase CLI"; "Pro Plan projects can access the last 7 days of daily backups"; "Pro, Team and Enterprise Plan projects can enable PITR as an add-on"; "Database backups do not include objects you store via the Storage API". Pricing: "Free projects are paused after 1 week of inactivity", "Log retention: 1 day", "500,000 [edge invocations] per month", DB 500 MB, storage 1 GB. Edge limits: wall clock "150s" Free vs "400s" paid. Live sizes: DB 45 MB, 108 public tables, 30 auth users, 7 projects, 14 storage objects (~10 MB) across 3 of 11 buckets.
- Failure scenario: the phase-2 column drop, a `db push` that replays `rls_baseline`, an accidental `DELETE`, or a leaked service key → every contractor's projects, invoices and cost book are gone with no restore path. Operationally: F1 could only be seen in a 6-hour (`net._http_response`) or 24-hour (`function_edge_logs`) window, which is why 40 days of 401s left no trace. The cron `timeout_milliseconds` of 180 s (morning-digest) and 240 s (homeowner-weekly-digest) exceed the 150 s Free wall clock — a fan-out that runs long is killed mid-send, after some emails went out and before `lastSentAt`/outbox markers are written.
- Fix: upgrade the org to Pro before launch (daily backups + 7-day logs; PITR add-on optional). Until then: a scheduled `supabase db dump --linked --data-only -f` + schema dump from a founder machine or GitHub Action, and `supabase storage cp -r ss:///<bucket> ./backup` for the three non-empty buckets. Record plan + backup cadence in START-HERE. Founder dashboard checks (all read-only): Organization → Billing → Plan; Project → Database → Backups → "Daily backups" tab (should list dates; on Free shows upgrade prompt) and "Point in time" tab (add-on state); Project → Settings → Infrastructure → compute size; Project → Logs → retention notice; Storage → each bucket → confirm no versioning.
- Effort: S (decision) / M (dump job).

### F5 — P2 CONFIRMED — `fetch-external-data` returns `{"success":true,"message":"Data fetch cycle complete"}` (HTTP 200, heartbeat fires) while SAM.gov rejects its key on every run; the federal bid feed has been stale since April.
- Where: `supabase/functions/fetch-external-data/index.ts:177-268` (SAM branch: a failed page logs and continues), `:468-470` (heartbeat fires unconditionally on completion).
- Evidence (live `function_logs`, 2026-09-03 18:00 and 22:00): `keys present → sam: true adzuna: true google: true` → `[sam] page failed 401 <html><body><h1>API_KEY_INVALID</h1>…` → `[sam] total opportunities pulled: 0` → `=== Data fetch cycle complete ===`; `net._http_response` 200 `{"success":true,"message":"Data fetch cycle complete"}`. `cached_bids` federal rows: 40, newest `posted_date` **2026-04-10** (129 other rows, newest 2026-06-22). Also `Skipping Google Places — last run 5d ago` (by design).
- Failure scenario: Discover → Bids shows five-month-old federal opportunities; nothing alerts; if `BETTERSTACK_HEARTBEAT_FETCH_EXTERNAL` is set the monitor says healthy.
- Fix: rotate `SAM_GOV_API_KEY`; return non-200 (and skip the heartbeat) when any provider answers 401/403; write `last_success_at` per source (a tiny `ops_source_health` row) and show "updated N days ago" in the bids screen.
- Effort: S.

### F6 — P2 CONFIRMED — Production Sentry events are unsymbolicated: `SENTRY_DISABLE_AUTO_UPLOAD=true` is baked into the production build profile, and no runbook uploads sourcemaps after `eas update`, so JS stacks from OTA'd code are Hermes bytecode offsets.
- Where: `eas.json:28` (`"SENTRY_DISABLE_AUTO_UPLOAD": "true"` under `build.production.env`); `app/_layout.tsx:132-165` (`Sentry.init`: hardcoded DSN — fine, DSNs are public; `enabled: !__DEV__`; `environment`; `tracesSampleRate: 0.1`; `sendDefaultPii: false`; `enableLogs: false`; `replaysSessionSampleRate: 0.1`, `replaysOnErrorSampleRate: 1`; `mobileReplayIntegration()` + `feedbackIntegration()`; no `beforeSend`, no explicit `release`/`dist`, `enableNative` default on); repo-wide grep for `sentry-expo-upload-sourcemaps`/`sentry-cli` → 0 hits outside package-lock.
- Evidence: `ios/sentry.properties` is untracked (ios/ gitignored) and reads `# Using SENTRY_AUTH_TOKEN environment variable`; `.env.local` (gitignored, untracked — verified with `git ls-files`) holds `SENTRY_AUTH_TOKEN`. Capture sites that will be unreadable: `components/ErrorBoundary.tsx:35`, `utils/offlineQueue.ts:164-166` (dropped writes) and `:475-479` (terminal write errors), `utils/photoUploadQueue.ts:90-91`.
- Failure scenario: the first production crash (e.g. the reanimated trap that silently rolled back build #12) arrives as `index.android.bundle:1:48213`-style frames; the "dropped N writes" warnings — the one signal for silent data loss — cannot be attributed to a screen.
- Fix: drop `SENTRY_DISABLE_AUTO_UPLOAD` from the production profile (keep the token as an EAS secret), and add `npx sentry-expo-upload-sourcemaps dist` immediately after every `eas update --branch production` in DEPLOY-RUNBOOK/DEPLOY-VERIFIED.
- Effort: S.

### F7 — P2 CONFIRMED — Universal links are not configured: no `associatedDomains`, no AASA file (the URL returns the SPA's `index.html` as `text/html`), no `assetlinks.json` — every email CTA (`https://app.mageid.app/…`, `notify/index.ts` `APP_BASE`) and every shared link opens Safari, never the installed app.
- Where: `app.json` (`scheme: "mageid"`, router origin `https://app.mageid.app/`, no `ios.associatedDomains`, no `android.intentFilters`); `ios/MAGEID/MAGEID.entitlements` (only `aps-environment` + `applesignin`); `public/` has no `.well-known/`; `netlify.toml` SPA rule `/* → /index.html 200` captures the path.
- Evidence: `curl https://app.mageid.app/.well-known/apple-app-site-association` → `200 text/html; charset=UTF-8`, body `<!DOCTYPE html>…`; `https://mageid.app/.well-known/apple-app-site-association` → 404; `assetlinks.json` → 200 (also index.html).
- Failure scenario: GC taps "View change order" in the CO-approved email on their iPhone → Safari loads the RN-web app and asks them to sign in; the in-app deep-link handling (`utils/pendingDeepLink.ts`, quick actions) never runs.
- Fix: add `public/.well-known/apple-app-site-association` (`{"applinks":{"details":[{"appIDs":["HKT2J284D2.com.mageid.app"],"components":[{"/":"/*"}]}]}}`) and `assetlinks.json`; in `netlify.toml` add a `[[headers]]` for `/.well-known/*` with `Content-Type = "application/json"` and a `[[redirects]]` for `/.well-known/*` with `force = false` ahead of the SPA rule; `ios.associatedDomains: ["applinks:app.mageid.app"]` (native rebuild).
- Effort: M (needs a native build).

### F8 — P2 CONFIRMED — `app.mageid.app` serves no security headers except HSTS and `X-Robots-Tag`: no CSP, no `X-Frame-Options`/`frame-ancestors`, no `X-Content-Type-Options`, no `Referrer-Policy`; and the content-hashed bundle chunks are served `max-age=0, must-revalidate`.
- Where: `netlify.toml:39-43` (only `X-Robots-Tag = "noindex"`); compare `marketing/netlify.toml:142-160` which has the full set (verified live on `mageid.app`).
- Evidence (live `curl -I https://app.mageid.app/`): `cache-control: public,max-age=0,must-revalidate`, `strict-transport-security: max-age=31536000` (dashboard default, no `includeSubDomains`), `x-robots-tag: noindex` — nothing else. A `_expo/static/js/web/*.js` chunk: `cache-control: public,max-age=0,must-revalidate`.
- Failure scenario: the login screen is framable (clickjacking); the tokenized routes (`/prequal-form?token=`, `/shared-schedule`, `/shared-photos`, `/shared-estimate`, `/shared-plan`, `/client-view`, `/claim-crew` — the `DESKTOP_SHELL_EXEMPT` list in `app/_layout.tsx:113-117`) depend on the browser default referrer policy; every visit re-validates ~20 MB of immutable chunks.
- Fix: copy the marketing header block into the root `netlify.toml` with a `connect-src` for `https://nteoqhcswappxxjlpvap.supabase.co wss://… https://api.revenuecat.com https://*.ingest.us.sentry.io https://us.i.posthog.com https://api.openweathermap.org https://exp.host` (and Stripe if the web checkout is used), `frame-ancestors 'none'`, `Referrer-Policy: no-referrer` (tokens in query strings), and `Cache-Control: public, max-age=31536000, immutable` for `/_expo/static/*` and `/assets/*` (index.html stays `max-age=0`).
- Effort: S.

### F9 — P2 CONFIRMED — Cron-scheduling SQL in the repo would break or double the live jobs if run: the two un-prefixed files re-create `daily-digest`, `invoice-dunning` and `geocode-bids` under the SAME names WITHOUT `x-cron-secret` (→ the functions' `isValidCron` returns 401) and add duplicates of the other four under old names; `DEPLOY-NOW.sql` and `20260826180000_portal_link_expiry_cron.sql` schedule the undeployed `portal-link-expiry-notice`, and DEPLOY-VERIFIED step 2's `db push` (which moves only the phase-2 file) WOULD apply the latter.
- Where: `supabase/migrations/schedule_all_crons.sql:33-130` and `schedule_daily_digest.sql:29-40` (headers `'{"Content-Type": "application/json"}'::jsonb` only; job names `coi-expiry-watch`, `fetch-external-data`, `geocode-bids`, `homeowner-weekly-digest`, `morning-digest`, `invoice-dunning`, `daily-digest`); live names are `coi-expiry-watch-daily`, `fetch-external-data-schedule`, `morning-digest-hourly`, `homeowner-weekly-digest-friday` (from `20260523130000_cron_secret_guard.sql`). `DEPLOY-NOW.sql:623-637`; `DEPLOY-RUNBOOK.md:45` ("Paste `DEPLOY-NOW.sql` into the Supabase SQL editor and run it") vs `DEPLOY-VERIFIED-2026-09-02.md:27-30` ("held back"); `DEPLOY-VERIFIED-2026-09-02.md:170-172` (`mv` only `20260827120000…` then `supabase db push`).
- Evidence: `list_migrations` has no `20260826180000` entry (the 09-03 applies were recorded as `20260903032100 portal_link_expiry_columns` etc.), so the CLI considers it pending. Un-prefixed files are ignored by `db push` (CLI requires `<timestamp>_name.sql`) but are exactly what an operator pastes.
- Failure scenario: paste `schedule_all_crons.sql` "to be safe" → daily-digest stops (401 at the function), invoice-dunning stays dead, morning-digest fires twice an hour under two names once F1 is fixed. `db push` per the runbook → `portal-link-expiry-notice-am/pm` jobs 404 twice a day forever (recorded as `succeeded`).
- Fix: delete `schedule_all_crons.sql` and `schedule_daily_digest.sql` (superseded), strip `DEPLOY-NOW.sql:623-637`, and change the runbook's step 2 to move BOTH `20260826180000_*` and `20260827120000_*` out before `db push` (or apply the expiry cron via MCP only after the function ships).
- Effort: S.

### F10 — P2 LIKELY — Expo push tokens are never validated or pruned: `sendPush` marks `push_status = 'sent'` on any HTTP 200 from Expo, but Expo answers 200 with a per-ticket `status: "error"` (`DeviceNotRegistered`) for dead tokens, and there is no receipts pass — so `profiles.push_token` keeps stale tokens forever and the outbox says "sent". (LIKELY: the Expo error-body shape is from Expo's published contract, not reproduced here.)
- Where: `supabase/functions/notify/index.ts:197-217` (`return { ok: r.ok, resp }` → `pushStatus = r.ok ? 'sent' : 'failed'`); `morning-digest/index.ts:281` (same pattern); deployed notify: 0 hits for `DeviceNotRegistered`/`getReceipts`; `utils/notifications.ts` never re-registers on token change; `contexts/NotificationContext.tsx:45` writes the token via `supabaseWrite` on registration only.
- Evidence: `profiles`: 3 with `push_token` (1 `ios`, `push_token_updated_at` 2026-05-12; 2 with null platform/updated_at); outbox `push_status` values present: `sent` / null only — never `failed`.
- Fix: in `sendPush`, treat `resp.data?.status === 'error'` as failed; on `DeviceNotRegistered` null the profile's `push_token`; optionally poll `/push/getReceipts` 15 min later from the cron.
- Effort: S.

### F11 — P2 CONFIRMED — Unsubscribe tokens are a 12-character FNV-1a hash keyed by a string literal in source, and `schedule-ical` falls back silently to a literal HMAC key when its secret is unset.
- Where: `supabase/functions/_shared/email.ts:303` (`const UNSUB_SECRET = 'mage-id-unsub-2026-rotate-on-leak'`), `:312-325` (`buildUnsubscribeToken`/`verifyUnsubscribeToken` — FNV-1a 64-bit, base36, 12 chars); `unsubscribe/index.ts:185-193` relies on that token to stop "anyone could POST {email, event_key:null} and globally suppress an arbitrary address"; `schedule-ical/index.ts:30` and `schedule-ical-url/index.ts:18` (`Deno.env.get('SCHEDULE_ICAL_SECRET') ?? 'mage-id-ical-fallback-rotate-on-leak'`).
- Failure scenario: anyone with repo access (or who reads the deployed bundle — `get_edge_function` returns `_shared/email.ts` verbatim) can globally suppress a competitor's or a client's address: their invoices, dunning and COI warnings stop with `email_status = 'suppressed_unsubscribed'` and no UI shows it. If `SCHEDULE_ICAL_SECRET` was never set, every calendar-feed token is mintable from a known constant (the ownership re-check at `schedule-ical/index.ts:111-114` limits this to the owner's own feed — still a silent-fallback smell).
- Fix: `UNSUB_SECRET = Deno.env.get('UNSUB_SECRET')` + HMAC-SHA256 via WebCrypto (the file's own comment at `:309-310` says so) with a fail-closed refusal when unset; same for `SCHEDULE_ICAL_SECRET`. Rotating invalidates old links — acceptable (List-Unsubscribe links are regenerated per email).
- Effort: S.

### F12 — P2 CONFIRMED — Nine collections are AsyncStorage-only with no Supabase mirror; they are wiped on every sign-out/tenant switch (by design) and are unrecoverable on device loss or reinstall: material cart, scope sheets, takeoff (+ field verification + corrections), managed properties, cash-flow data, Ask/voice history.
- Where: `contexts/MaterialCartContext.tsx`, `utils/scopeSheet.ts`, `utils/takeoffStorage.ts`, `utils/fieldVerificationStorage.ts`, `utils/takeoffCorrections.ts`, `contexts/PropertyContext.tsx`, `utils/cashFlowStorage.ts`, `utils/askHistory.ts`, `utils/voiceCommandExecutor.ts` — each has 0 references to `supabaseWrite`/`supabase.from`/`offlineQueue` and 2-6 `AsyncStorage.setItem/getItem` calls; `utils/localCacheKeys.ts:121-133` records the wipe as a "KNOWN COST, accepted deliberately" and names the fix ("give them a Supabase mirror").
- Failure scenario: a GC does a takeoff on a plan set on the site iPad; a magic-link re-auth (indistinguishable from a tenant switch per `AuthContext`'s own comment) or a phone replacement → takeoff, cash-flow forecast and scope sheets are gone; no backup of any kind covers them.
- Fix: mirror takeoff and cash-flow through `supabaseWrite` (they are the money-bearing ones); label the rest "stored on this device only" in their screens.
- Effort: M.

### F13 — P2 CONFIRMED — Nothing tells support what a user is running: no version/build/OTA identifier is rendered anywhere in the app, `expo-updates` is never called (no check, no reload, no `updateId`), and there is no diagnostics/export-logs affordance — while `Sentry.feedbackIntegration()` is loaded but never surfaced.
- Where: repo-wide grep over `app/` and `components/` for `Updates.updateId|Updates.runtimeVersion|Updates.channel|nativeApplicationVersion|nativeBuildVersion|expoConfig.version` → 0 hits; `expo-application` is not a dependency; `app.json` `updates` has only `url` (defaults: `checkAutomatically: ON_LOAD`, `fallbackToCacheTimeout: 0`) so an OTA is applied on the *second* cold launch (the "force-quit and relaunch twice" note in `DEPLOY-RUNBOOK.md:100-102`); `app/_layout.tsx:163` registers `feedbackIntegration()` with no caller of `Sentry.showFeedbackWidget`/`captureFeedback`.
- Failure scenario: "the schedule dates are wrong" — support cannot tell whether the device runs the pre-fix OTA; the founder cannot verify an OTA landed except by behaviour.
- Fix: Settings › About row: `Application.nativeApplicationVersion (nativeBuildVersion) · OTA ${Updates.updateId?.slice(0,8) ?? 'embedded'} · ${Updates.channel}`; on foreground `Updates.checkForUpdateAsync()` → "Restart to update" toast; a "Send diagnostics" button that calls `Sentry.captureFeedback` with the offline-queue length and last dropped-write reason attached.
- Effort: S.

### F14 — P2 LIKELY — Abuse/cost controls have three open edges: (a) `send-email` has no per-user or daily cap (25 recipients per call, unlimited calls) against a Resend account limit of 5 req/s and a plan quota shared with dunning/digests; (b) anonymous `notify` callers can push any of the five allow-listed events with attacker-controlled copy (`signer_name`, `contract_value`, `contract_title`, `question`) into a GC's inbox at 30/h per portal id — unlimited across portal ids and with no per-IP limit; (c) 10 `verify_jwt:false` endpoints have no limiter at all (`public-cost-index`, `seal-document`, `portal-mark-viewed`, `mcp`, `mcp-token`, `financing-redirect`, `financing-callback`, `schedule-ical`, `qbo-connect-callback`; `stripe-webhook`/`revenuecat-webhook` are signature-gated). There is no global Gemini spend cap — only per-user `MONTHLY_CAPS` (`_shared/auth.ts:292-367`, fail-closed metering — good). (LIKELY: no abuse was exercised.)
- Where: `send-email/index.ts:52-54, 232-249` (caps on size/recipients only); `notify/index.ts:222-234, 259-262`; grep counts of `rate_limit|rateLimit` per function.
- Evidence: `rate_limit_counters` live scopes: `magiclink:email:*`, `magiclink:ip:*`, `portal:*`, `widget:*` — only auth-magic-link, notify (portal), widget-estimate, public-lead-intake and validate-portal-passcode meter anything.
- Fix: `rateLimitCount('sendemail:user:<id>')` with a daily ceiling (e.g. 200) in `send-email`; `rateLimitCount('notify:ip:<cf_connecting_ip>')` for anon callers; a Google Cloud budget alert on the Gemini project (dashboard).
- Effort: S.

## Cron / job inventory (live `cron.job`, `cron.timezone = GMT`)

| jobid | jobname | schedule (UTC) | target fn | deployed | verify_jwt | last 24 h | idempotency | note |
|---|---|---|---|---|---|---|---|---|
| 4 | fetch-external-data-schedule | 0 6,12,18,22 * * * | fetch-external-data v32 | yes | false | 200 ×4 (one pg_net timeout at 18:00 — job sets no `timeout_milliseconds`, default 5 s; function still completed) | upserts by `notice_id`/`external_id`/`place_id` | SAM key invalid (F5); `Authorization: Bearer <NULL setting>` is dead weight |
| 5 | daily-digest | 0 13 * * * | daily-digest v10 | yes | false | 200; outbox `daily_digest_sent` row 13:00:05 daily (83 rows, 1 recipient) | `alreadySentToday` outbox marker (UTC midnight) | works |
| 7 | morning-digest-hourly | 5 * * * * | morning-digest v9 | yes | **true** | **401 ×24** | none per day — matches user's local hour only; a manual re-fire in the same hour double-sends | F1 |
| 8 | homeowner-weekly-digest-friday | 0 21 * * 5 | homeowner-weekly-digest v9 | yes | false | last fired Fri 2026-08-28 (17 runs) | `client_portal.weeklyDigest.lastSentAt` < 6 days skip | 0 projects have it enabled; cron timeout 240 s > Free 150 s wall clock |
| 9 | coi-expiry-watch-daily | 0 13 * * * | coi-expiry-watch v6 | yes | false | 200 | per-sub per-threshold marker | 21 subs with a COI expiry |
| 12 | geocode-bids | 0 8 * * * | geocode-bids v11 | yes | false | 200 | cache-first | fine |
| 15 | invoice-dunning | 0 15 * * * | invoice-dunning v5 | yes | **true** | **401** | `dunning_stage` written only after confirmed send | F1; 3 overdue unpaid invoices waiting |
| 23 | qbo-reconciler-every-30m | */30 * * * * | qbo-reconciler v2 | yes | **true** | **401 ×48** | idempotent upsert + prior-status skip | F1; 1 QBO connection |
| — | (none) | — | portal-link-expiry-notice | **not deployed** | — | — | outbox `event_type` per portal, 20 h cooldown; writes `recipient_kind: "gc"` (fixed) | only `DEPLOY-NOW.sql:623-637` and `20260826180000_*` point at it (F9); nothing else does |

Deployed but not in the repo: `sync-bids` (v23, 2026-04-10), `fetch-material-price` (v21, 2026-04-12) — no client caller found in `app/ components/ contexts/ hooks/ utils/ lib/`; both `verify_jwt: true`. Dead code to delete from the project (they still run if invoked).

## Notification pipeline — end to end

- **Sources that work:** anon-allow-listed events POSTed by the static portal page with the anon key (`marketing/portal/index.html:3610-3623` → `contract_signed`, `selection_chosen`; note `:6635` posts `portal_reaction`, which notify's `switch` does not know → `unknown_event`, harmless); service-role callers (`award-rfp` → `rfp_awarded`, `notify-nearby-contractors` → `nearby_rfp_posted`, itself triggered by `public_bids_notify_nearby_fn` **with** `x-cron-secret`); the app's `utils/notifyClient.ts:51-77` with the user's JWT (GoTrue-verified → privileged) for `closeout_binder_sent` and `bid_question_*`.
- **Sources that are dead:** every DB trigger through `fire_notify` and `notify_portal_message_fn` (F2).
- **Outbox semantics:** `notification_outbox` is written **after** the send attempt as an audit/inbox row, not a queue — there is no drain, no retry, no dead-letter state. Live statuses: `email_status ∈ {sent, null}`, `push_status ∈ {sent, null}`; 0 `failed`, 0 `suppressed_unsubscribed`; 128 rows total, 83 of them `daily_digest_sent` markers (hidden from the inbox by `useNotificationFeed.ts:66`). A failed Resend call yields a `failed` row that nothing ever retries; a failed outbox insert is `console.log`ged and dropped (`notify/index.ts:411`). `recipient_kind`: every writer in the repo and in the deployed bundle writes `gc|client|sub`; the app never inserts into the table (reads/updates/deletes only, `useNotificationFeed.ts:56-96`); `scripts/validate-outbox-contract.ts` pins it — no evidence of any swallowed 23514 exists in the outbox or logs, and START-HERE's open decision #4 ("the app writes 'user'") no longer describes any code path.
- **Preferences:** `profiles.notification_preferences` (`prefAllows`, `notify/index.ts:181-189`; 1 profile has any set) + `digest_*` columns; **push:** Expo push via `exp.host`, token stored on `profiles.push_token` (`NotificationContext.tsx:45` via `supabaseWrite`), no receipts/pruning (F10). **Email:** `_shared/email.ts` `resendSend` with personalised FROM (`"{Company} via MAGE ID <noreply@mageid.app>"`), `List-Unsubscribe` + `List-Unsubscribe-Post: List-Unsubscribe=One-Click`, plaintext fallback, 429 retry/backoff; pre-send suppression via `is_email_unsubscribed` (fail-open). **Unsubscribe:** signed link (weak key, F11) → `mageid.app/unsubscribe` (301 → `/unsubscribe/`) / `/preferences` static pages → `unsubscribe` fn (token required for both directions since the fix at `unsubscribe/index.ts:185-193`); 1 live suppression row (`portal_message`, `one_click`, 2026-04-30). **CAN-SPAM:** footer carries sender line + unsubscribe + "Powered by MAGE ID" but **no physical postal address** (`email.ts:353-372`); transactional/relationship mail is exempt, but `nearby_rfp_posted` (marketplace fan-out to contractors who did not opt in) is the one category that reads as commercial — add the address line there.

## Secrets & environment inventory

Server (edge-function secrets; `Deno.env.get` enumerated across all 61 functions + `_shared`):

| Var | Used by | Behaviour when missing |
|---|---|---|
| `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY` (+ legacy alias `SERVICE_ROLE_KEY`), `SUPABASE_ANON_KEY`/`ANON_KEY` | all | platform-injected; `|| ''` fallbacks make REST calls 401 (loud). **Silent:** `fetch-external-data:159` and `geocode-bids:85` fall back to the ANON key → upserts fail under RLS while the run still reports success. 9 functions hard-code the project URL as fallback (harmless). |
| `GEMINI_API_KEY` | 18 fns (`ai`, all `analyze-*`, `compare-drawings`, `import-schedule`, `plan-extract`, `portal-ask-home`, `safety-*`, `scan-*`, `homeowner-weekly-digest`, `morning-digest`, `_shared/embeddings`) | `|| ''` → upstream 4xx → error responses; no fake data path found |
| `ANTHROPIC_API_KEY` | `construction-answer`, `analyze-takeoff` | `not_configured` (known, inert by design) |
| `RESEND_API_KEY` | `notify`, `send-email`, `daily-digest`, `morning-digest`, `homeowner-weekly-digest`, `coi-expiry-watch`, `invoice-dunning`, `auth-magic-link`, `project-invite`, `stripe-webhook` | `send-email` → 500 "Email service not configured"; the others mark `email_status='failed'` and return 200 |
| `EXPO_ACCESS_TOKEN` | `notify`, `morning-digest` | optional (lower Expo push rate limit) |
| `STRIPE_SECRET_KEY` (4), `STRIPE_WEBHOOK_SECRET` | connect-*, create-payment-link, create-rfp-checkout, stripe-webhook | `|| ''` — webhook signature check with an empty secret not traced (see "could not verify") |
| `REVENUECAT_WEBHOOK_SECRET`, `REVENUECAT_SECRET_API_KEY` | revenuecat-webhook | unset (known BLOCKER, `LAUNCH-CHECKLIST.md:11`) → 500 |
| `INTUIT_CLIENT_ID/SECRET/REDIRECT_URI/ENVIRONMENT/STATE_SECRET` | qbo-* (5) | `STATE_SECRET` silently falls back to `CLIENT_SECRET` (`_shared/qbo.ts:203`); `REDIRECT_URI` defaults to prod URL |
| `SCHEDULE_ICAL_SECRET` | schedule-ical, schedule-ical-url | **silent literal fallback** (F11) |
| `FINANCING_CALLBACK_SECRET`, `FINANCING_THANKYOU_URL`, `FINANCING_FALLBACK_URL` | financing-* | `|| ''` / prod-URL defaults |
| `OPENWEATHER_API_KEY` (server) | morning-digest | returns `null` weather — no simulated data (good) |
| `CLOUDCONVERT_API_KEY` | convert-pdf-to-images | `?? ''` |
| `PEXELS_API_KEY` | og-image | optional |
| `SAM_GOV_API_KEY`, `ADZUNA_APP_ID/KEY`, `GOOGLE_PLACES_API_KEY` | fetch-external-data, geocode-bids | SAM key present but **invalid** (F5) |
| `BETTERSTACK_HEARTBEAT_DAILY_DIGEST/_FETCH_EXTERNAL/_GEOCODE` | 3 fns only | optional; **no heartbeat exists for morning-digest, invoice-dunning, qbo-reconciler, coi, homeowner** — the three that are dead |
| `APP_ORIGIN`, `INVITE_FROM_EMAIL` | project-invite | prod defaults |
| `PLATFORM_FEE_BPS`, `_FREE/_PRO/_BUSINESS/_ENTERPRISE`, `RFP_POST_FEE_CENTS` | create-payment-link, create-rfp-checkout | **business numbers hidden in env defaults** (50/0/30/50/40 bps, 2,500 ¢) — nowhere documented |

`docs/phase26-functional-audit.md` (the file `CLAUDE.md:139` points to for server secrets) lists 8 of these ~40 names; `BETTERSTACK_*`, `INTUIT_*`, `SCHEDULE_ICAL_SECRET`, `FINANCING_*`, `REVENUECAT_*`, `PEXELS`, `SAM_GOV`, `ADZUNA`, `APP_ORIGIN`, `INVITE_FROM_EMAIL`, `PLATFORM_FEE_BPS_*`, `RFP_POST_FEE_CENTS` are undocumented. Which of them are actually set in production cannot be listed through the MCP (founder: Dashboard → Edge Functions → Secrets, screenshot the names).

Client (`EXPO_PUBLIC_*`, inlined by Metro): `SUPABASE_URL`/`ANON_KEY` (public; hard-coded fallbacks in `lib/supabase.ts:16-17`, `app/client-portal-setup.tsx:66-67`, `app/project-detail.tsx:105-106`, `app/sub-portal-setup.tsx:40-42`), `REVENUECAT_IOS/ANDROID/WEB/TEST_API_KEY` (public SDK keys; committed in `eas.json` on both profiles; web key is sandbox `rcb_sb_` — known), `OPENWEATHER_API_KEY` (absent in `eas.json` → simulated, labelled), `POSTHOG_KEY/HOST` (hard-coded default `phc_t7H3…` in `utils/posthog.ts:20` — write-only key, same key used by the marketing site's 13 pages), `PROJECT_ID` (from `app.json` extra). `.env` and `.env.local` are gitignored and untracked (`git ls-files` returns neither). No real secret found in client code, `app.json`, or `eas.json`. Two tracked local-state files, `.netlify/netlify.toml` and `marketing/.netlify/netlify.toml`, embed absolute paths from this Mac — harmless but should be gitignored.

## Observability summary

- **Sentry:** initialised once (`app/_layout.tsx:132-165`), `Sentry.wrap(RootLayout)` at `:1360`; ErrorBoundary forwards with component stack; offline-queue and photo-queue drops are `captureMessage`d — but unsymbolicated (F6). Mobile replay at 10 % of sessions with default masking; `sendDefaultPii: false` — the privacy-policy note in the code (`:147-151`) is still open. Web: same init runs on RN-web (Sentry RN SDK supports it) — fine.
- **Edge functions report nowhere except Supabase logs** (1-day retention on Free). Three functions optionally ping BetterStack on success; nothing pings on failure. `console.error` in `notify`/`send-email` is the only trail.
- **Analytics:** PostHog over raw HTTP (`utils/posthog.ts`), identity stitched on login/logout (`_layout.tsx:250-261`). Emitted today (19): `USER_LOGGED_IN, ESTIMATE_SHARED, ESTIMATE_GENERATED (with used_learned_costs/learned_rate_count/jobs_analyzed from utils/activationSignals.ts), SUBSCRIPTION_PURCHASED, PAYWALL_DISMISSED, COST_RATES_SEEDED, USER_SIGNED_UP, SUBSCRIPTION_PURCHASE_STARTED/_FAILED, SCHEDULE_GENERATED, PROJECT_CREATED, PERSONA_SELECTED, PAYWALL_VIEWED, ONBOARDING_RATES_VIEWED/_SKIPPED/_COMPLETED, MATERIAL_RECEIPT_SAVED, INVOICE_CREATED, CONTRACTOR_INVITE_SHARED`. Declared but never emitted (15): `BID_POSTED, CHANGE_ORDER_CREATED, CONTACT_ADDED, DAILY_REPORT_CREATED, EQUIPMENT_ADDED, MESSAGE_SENT, ONBOARDING_IMPORT_VIEWED/_COMPLETED/_SKIPPED, PDF_GENERATED, PHOTO_ADDED, PUNCH_ITEM_CREATED, RFI_CREATED, SUBMITTAL_CREATED, USER_LOGGED_OUT` — the "import-your-pipeline" activation step documented at `utils/analytics.ts:61-73` has no emitter; the PostHog activation funnel (signup → persona → estimate_generated[used_learned_costs]) is intact.
- **Cron:** `cron.job_run_details` is the only durable record and it cannot see HTTP outcomes (F1). `net._http_response` is the truth but lives 6 h.

## Recoverability

- **Database:** Free plan → no backups, no PITR (F4). 45 MB; 30 users; 7 projects.
- **Formerly-orphaned tables:** `contractor_licenses`, `delivery_receipts`, `draw_periods`, `owner_supplied_items`, `workers`, `sub_change_requests`, `time_entries`, `permit_templates` all exist live with RLS enabled and 4 policies each; their DDL now lives in `20260819160000_orphaned_tables_schema_backfill.sql` (`REPAIR-HISTORY.sh:16` marks it PARTIAL — "4 owner_supplied_items policies absent" — live shows 4 policies on that table today; re-diff policy names before marking the migration applied). `list_migrations` does not record `20260819160000`.
- **Storage:** 11 buckets, 3 non-empty (`plan-sheets` 7 objects 9.7 MB, `rfp-attachments` 5, `project-documents` 2); `profiles`, `rfp-attachments`, `plan-sheets` are `public: true`. Never included in DB backups — needs its own export.
- **Device-only data:** F12 list; plus `mageid_offline_queue` / `mageid_photo_upload_queue` (pending writes — by definition unrecoverable until flushed).
- **Migration replay is itself a hazard:** `REPAIR-HISTORY.sh` deliberately excludes `cost_seeds` and `rls_baseline`; a naive `db push` would replay them.

## ADD / CONNECT / DO BETTER (ranked by leverage)

### O1 — A cron-health record that outlives pg_net's 6-hour TTL — leverage: 40 days of 401s were invisible; the next silent failure will be too — evidence of the gap: `cron.job_run_details` 0 non-success vs `net._http_response` 18/20 401 — sketch: a `private.cron_http_log(created, status_code, body_head)` table filled by a 5-minute pg_cron job (`insert … select from net._http_response where status_code is distinct from 200 and created > now() - interval '10 min'`), a `cron_health` view (last status per job by time-slot), and a daily line in `daily-digest`'s founder email ("3 jobs failing"). Free-plan-proof (lives in Postgres).
### O2 — `supabase/config.toml` + a ship-check guard on live `verify_jwt` — leverage: F1 and F3 are the same root cause and it has already recurred three times — evidence: no config.toml; `docs/deploy/2026-07-07-production-handoff.md:72-74` deferred it — sketch: per-function `[functions.<name>] verify_jwt = <bool>` (list in F3), `scripts/validate-edge-verify-jwt.ts` diffing `supabase functions list` output against it.
### O3 — "Send test notification" + last-delivery timestamps on `app/notifications-settings.tsx` — leverage: would have surfaced F2 the first time anyone toggled a preference — evidence: no self-test path exists; the inbox is the outbox — sketch: button → `notifyEvent('closeout_binder_sent' …)`-style privileged call with a `test: true` payload → expect an outbox row within 5 s; show `max(created_at)` per channel.
### O4 — Pro plan + weekly `pg_dump` + storage export (F4) — leverage: the only thing that turns a bad migration from fatal into a 30-minute restore — sketch: GitHub Action on cron with `SUPABASE_ACCESS_TOKEN`, artefacts to a private bucket; `START-HERE` records the last verified restore.
### O5 — Settings › About with version/OTA id + "Send diagnostics" (F13) — leverage: every support conversation starts with "which build?" — sketch: `expo-application` + `expo-updates` values, `Sentry.captureFeedback` with queue state attached.
### O6 — Universal links (F7) — leverage: every email CTA currently ejects the GC into Safari — sketch: AASA + associatedDomains + assetlinks; ship with the next native build.
### O7 — Mirror takeoff + cash-flow to Supabase (F12) — leverage: they are the two device-only collections that carry money — sketch: `supabaseWrite('takeoffs', …)` keyed by project, rehydrate on sign-in.

## Appendix — lower-severity notes (one line each with file:line)

- P3 — `docs/START-HERE.md:39` "invoice-dunning is STILL emailing clients a FINAL NOTICE on every run" — false; it has not executed since 2026-08-03 (F1). Same doc's open decision #4 (`recipient_kind 'user'`) describes no live code path.
- P3 — `supabase/migrations/20260523130000_cron_secret_guard.sql:1-3` and `_shared/cronAuth.ts:3-4` state the cron functions "deploy with verify_jwt=false" — three do not; `schedule_all_crons.sql:16-18` claims every function is outbox-idempotent — `morning-digest` has no per-day marker.
- P3 — `DEPLOY-RUNBOOK.md:72` deploys `portal-link-expiry-notice` while `DEPLOY-VERIFIED-2026-09-02.md:27-30` holds its cron back — the two runbooks disagree on order; `DEPLOY-RUNBOOK.md:45` (paste `DEPLOY-NOW.sql`) schedules the cron anyway (`DEPLOY-NOW.sql:623-637`).
- P3 — `LAUNCH.md:201` references `lib/stripe.ts` (does not exist; `lib/` contains only `supabase.ts`); `LAUNCH.md:195` "No third-party trackers" — PostHog + Sentry session replay are third-party analytics/diagnostics and belong on the App Privacy label.
- P3 — `LAUNCH-CHECKLIST.md` has no item for backups/plan, `config.toml`, cron health, sourcemaps, or SAM.gov key rotation.
- P3 — `cron.job` jobid 4 sets no `timeout_milliseconds` (pg_net default 5 000 ms → 1 of 4 runs today logged `Timeout of 5000 ms reached`); `schedule_all_crons.sql:59` intended 120 000.
- P3 — `notify/index.ts:222-234` anon rate limiter and `isUnsubscribed` are fail-open by design; `_shared/auth.ts:193-207` metering is fail-closed — document the split so nobody "fixes" one to match the other.
- P3 — `notify/index.ts:807-811` `bid_question_answered` writes `push_status: 'sent'` / `email_status: 'sent'` unconditionally, before checking the send result.
- P3 — `daily-digest` fires at 13:00 UTC = 09:00 EDT / 08:00 EST; `schedule_daily_digest.sql:1` says "8 AM EST"; `homeowner-weekly-digest-friday` runs 21:00 UTC live vs "16:00 UTC (11 AM EST)" in `schedule_all_crons.sql:80-81`.
- P3 — `marketing/portal/index.html:6635` posts `event: 'portal_reaction'`, which `notify` does not handle (`unknown_event`); dead feature or missing branch.
- P3 — `send-email/index.ts:198-212` identifies the caller by a bare JWT decode (relies on the gateway's `verify_jwt: true`); `DEPLOY-VERIFIED` notes the same for `award-rfp` — fine only while F3's config.toml keeps those functions `true`.
- P3 — `app/_layout.tsx:147-151`: `sendDefaultPii: false` with a code comment that the privacy policy does not yet disclose Sentry replay/IP — a policy item, not code.
- P3 — `utils/posthog.ts:20` default key and `marketing/*.html` share one PostHog project; web-app pageviews are not captured (HTTP provider has no autocapture) — expected, but the marketing snippet's autocapture will make funnel counts look marketing-heavy.
- P3 — HSTS on `app.mageid.app` lacks `includeSubDomains`/`preload` (dashboard-managed); `mageid.app`'s `includeSubDomains` already covers `app.` on second visit.
- P3 — `mageid.app/portal/anything` returns 200 for any id (SPA rule) — expected; `mageid.app/unsubscribe` is a 301 to `/unsubscribe/` — Gmail one-click POSTs the `List-Unsubscribe` URL directly to the function, so the static page's redirect does not matter.
- P3 — Deployed-only functions `sync-bids` and `fetch-material-price` (no source in repo, no caller) should be deleted from the project.
- P3 — `README`-level: `CLAUDE.md:139` points to `docs/phase26-functional-audit.md` for server secrets; that table covers 8 of ~40 names (see inventory).

## What I could not verify (and how it could be)

- **Which edge-function secrets are actually set** (Resend, Gemini, Stripe, Intuit, BetterStack, SCHEDULE_ICAL_SECRET, FINANCING_CALLBACK_SECRET, UNSUB…): the MCP has no `list_secrets`. Founder: Dashboard → Edge Functions → Secrets (names only). `RESEND_API_KEY` is evidently set (daily digests deliver with Resend ids in `email_response`).
- **Behaviour of `stripe-webhook`/`financing-callback` with an empty secret**: not traced; check `stripe-webhook/index.ts` signature path with `STRIPE_WEBHOOK_SECRET = ''` before relying on it.
- **Expo push error-body shape** (F10): from Expo's documented ticket format; not reproduced against a dead token.
- **Resend plan limits / bounce & complaint rates**: no Resend access. Founder: Resend → Domains (SPF/DKIM/DMARC status) and Usage.
- **RevenueCat webhook still "Server not configured"**: not re-probed (an unauthenticated POST is harmless but is still a write-shaped call); `LAUNCH-CHECKLIST.md:24` says verified 2026-08-08.
- **Whether pg_cron's pg_net calls count as "activity" for the Free-plan 7-day pause**: unknown; Supabase counts API-gateway traffic — the cron's hits to `/functions/v1/*` probably qualify, but do not rely on it (F4).
- **The exact policy set on `owner_supplied_items`** vs the 08-19 backfill file (REPAIR-HISTORY says 4 absent; live shows 4 present — names not diffed).
- **Whether the marketing site is deployed from `marketing/netlify.toml` or `_redirects`**: both exist and agree on the 404 catch-all (live: 404 on an unknown path, CSP present); the tracked `marketing/.netlify/netlify.toml` snapshot (2026-08-15) still shows a `/* → /index.html 200` fallback — stale local state, not live behaviour.
- **Edge-function log history beyond 24 h and pg_net responses beyond 6 h** — gone; the since-July run counts are from `cron.job_run_details`, whose `succeeded` status was proven meaningless for HTTP outcome.
