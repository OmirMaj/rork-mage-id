# App Store submission risk & release engineering — final-push audit — 2026-09-03

## Scope covered (files/paths actually read; commands run)

**Config / native:** `app.json`, `eas.json`, `package.json`, `bun.lock` (reanimated/maps entries), `babel.config.js`, `metro.config.js`, `netlify.toml`, `.gitignore`, `plugins/withGoogleModularHeaders.js`, `plugins/withQuotedXcodeScriptPaths.js`, the untracked local prebuild `ios/` (Aug 20: `MAGEID/Info.plist`, `MAGEID.entitlements`, `PrivacyInfo.xcprivacy`, `Supporting/Expo.plist`, `Podfile`, `Podfile.properties.json`, `sentry.properties`, Pods privacy manifests), `assets/images/icon.png|splash-icon.png|adaptive-icon.png` (sips), `public/.well-known/`.
**App code:** `app/_layout.tsx` (Sentry init, splash/fonts, auth gate, quick actions, magic-link handler, offline drain, Stack), `app/paywall.tsx`, `app/onboarding-paywall.tsx`, `components/ClientPaywall.tsx`, `hooks/useClientPaywall.ts`, `contexts/SubscriptionContext.tsx`, `contexts/AuthContext.tsx` (bootstrap, signup/reset/magic-link redirects, Google native, Apple native, deleteAccount), `contexts/ProjectContext.tsx` (boot queries), `contexts/NotificationContext.tsx`, `utils/notifications.ts`, `utils/brief/nudge.ts`, `utils/weekClose/nudge.ts`, `components/home/MorningBriefCard.tsx`, `components/home/WeekCloseCard.tsx`, `app/login.tsx`, `app/signup.tsx`, `app/(tabs)/settings/index.tsx`, `app/dev-seeder.tsx`, `app/dev-flagship-seeder.tsx`, `app/cost-seed.tsx`, `utils/owner.ts`, `utils/posthog.ts`, `utils/signupIntent.ts`, `utils/deepLinkScheme.ts`, `app/+native-intent.tsx`, `app/post-rfp.tsx`, `utils/location.ts`, `utils/photoGeoStamp.ts`, `components/TakeoffFieldVerifyButton.tsx`, `components/VoiceCaptureModal.tsx`, `components/ErrorBoundary.tsx`, `utils/aiLimitAlert.ts`, `lib/supabase.ts`, `scripts/validate-import-tracking.ts`.
**Server:** `supabase/functions/create-rfp-checkout/index.ts`, `delete-account/index.ts`, `revenuecat-webhook/index.ts`, `notify/index.ts`, `_shared/auth.ts`.
**Docs:** `CLAUDE.md`, `docs/START-HERE.md` (banner), `LAUNCH-CHECKLIST.md`, `DEPLOY-VERIFIED-2026-09-02.md`, `docs/app-store-metadata.md`, `docs/deploy/2026-08-19-release-build-prep.md`, `docs/deploy/2026-08-19-session-two-recap.md`, `docs/audits/2026-09-02-launch-readiness.md` (#2), `docs/PRODUCT-BIBLE.md` §12.
**Commands (all read-only):** `git ls-files ios`, `git log -S…` (EAS project id, version 1.1.0 bump/revert, `EXPO_NO_CAPABILITY_SYNC`, `SENTRY_DISABLE_AUTO_UPLOAD`, reanimated add/remove, shortcut config), `git show <build-commit>:<file>` for builds #11–#14 and OTA 317f7795; `npx expo config --type introspect --json` (resolved Info.plist/entitlements, no files written); `npx expo export --platform ios --output-dir <scratchpad>` (21.4 MB Hermes bundle scanned for reanimated/maps markers; repo verified clean afterwards); `eas whoami`, `eas build:list --platform ios --limit 4 --json` (build numbers, commits, fingerprints, expiry), `eas update:list --branch production`, `eas update:view 7e79cdba…`, `eas env:list --environment production`; `curl` probes of `mageid.app/privacy`, `/terms`, `app.mageid.app/.well-known/apple-app-site-association`, and an unauthenticated POST to the deployed `revenuecat-webhook` (returns before any side effect). No production SQL was needed.

Facts corrected along the way: the real EAS project is `00860b05-b25b-4b37-b85a-846ecb80bb4c` (app.json, `updates.url`, Expo.plist), not the `9f6536e0…` in CLAUDE.md; `ios/` is **not** tracked (`.gitignore:15`), so there is no tracked-prebuild drift — EAS prebuilds fresh.

## Findings (ranked; most severe first)

### F1 — [P0] [CONFIRMED] Every existing native build (#11–#14) and the newest production OTA embed JS that predates the 2026-09-02 App Store blocker fixes; submitting any existing build puts the 3.1.1 client paywall and the 1.2 RFP feed in front of App Review on first launch, and no deploy document calls for a new native build.
- Where: `eas build:list` → #11 `998f8aef` (08-20), #12 `3a4c5030` (08-20), #13 `3bac6616` (08-21), #14 `4ed73864` (08-23), all `runtimeVersion 1.0.0`, `channel production`, `distribution STORE`; `git show <each>:components/ClientPaywall.tsx` → **0** references to `CLIENT_SUBS_ENABLED`; `git show <each>:app/(tabs)/mage-id-bids/index.tsx` → **0** references to `RFP_BROWSE_ENABLED`; both flags were introduced in `bd6721e4` (2026-09-02 01:11). Latest production update group `7e79cdba…` = commit `317f7795` (08-25, "disable unenforced RFP paid path") — same 0/0. `ios/MAGEID/Supporting/Expo.plist`: `EXUpdatesCheckOnLaunch = ALWAYS`, `EXUpdatesLaunchWaitMs = 0` (a fresh install's first launch runs the embedded bundle; an OTA applies on the *next* cold launch). `DEPLOY-VERIFIED-2026-09-02.md:236-244` step 5 is `eas update` only; the grep for "eas build / new build / native build" across the deploy docs returns nothing but the reanimated remark. `docs/START-HERE.md`: "the OTA. Every app-side fix from those audits is in the repo and not on any device."
- Evidence (pre-fix behaviour, as the shipped bundles still have it): `docs/audits/2026-09-02-launch-readiness.md:22-28` — "Tapping 'Start 14-day free trial' silently unlocks the feature with no purchase sheet. That is a 3.1.1 rejection"; `hooks/useClientPaywall.ts:80-97` is the fix that is *only in git*.
- Failure scenario: founder runs `eas submit --platform ios --latest` (= #14) → reviewer installs → first cold launch = embedded Aug-23 JS → chooses the Homeowner persona → "Post a Project" → modal quoting $19/mo and $49/mo with "Start 14-day free trial" (AsyncStorage unlock) → 3.1.1 + 2.3.1; Bids tab shows strangers' RFPs with no report/block → 1.2. Publishing the OTA first does not help the reviewer's first launch.
- Fix: build **#15 from HEAD** (`eas build --profile production --platform ios`) and submit that binary; publish the OTA as well for the TestFlight installs; add "native build from HEAD" as step 5a of DEPLOY-VERIFIED and make it the standing rule whenever a submission-blocker fix is JS-only.
- Effort: S

### F2 — [P1] [LIKELY] The build-#12 reanimated boot-crash trap is re-armed: the fix dropped `react-native-reanimated` from package.json only; `bun.lock` and `node_modules` still carry 4.1.7, an `expo export` from this checkout bundles it again, DEPLOY-VERIFIED's "not in play" claim is false, and three native fingerprints share runtime `1.0.0` with no guard.
- Where: `bun.lock:1840` `"react-native-reanimated": ["react-native-reanimated@4.1.7", …]` (also `bun.lock:1168`, an optional peer of `expo-router`); `node_modules/react-native-reanimated/package.json` = 4.1.7 (mtime Aug 20, the day `3bac6616` added it; `82b24e50` removed only the package.json line); exported bundle `entry-115a311b….hbc` (21.4 MB) contains `Native part of Reanimated` ×1, `initializeReanimatedModule`, `__reanimatedModuleProxy`, `makeShareableCloneRecursive`, `react-native-reanimated` ×11 — `82b24e50`'s verified-clean bundle was 20.1 MB with those strings at 0; `eas build:list` fingerprints: #11 `fb51c1a8…`, #12 `ba6d0a07…`, #13/#14 `38b22d6f…` (#13/#14 commits list reanimated in package.json; #11/#12 do not); `app.json` `"runtimeVersion": { "policy": "appVersion" }`; `DEPLOY-VERIFIED-2026-09-02.md:242-244` "No new native dependencies … the reanimated runtime trap … is not in play. `expo export --platform ios` was verified exit 0 at 21.4 MB" (exit 0 and 21.4 MB is exactly the contaminated size); `app/_layout.tsx:275-277` "adding one is a native dep and would break OTA".
- Why LIKELY, not CONFIRMED: today the only boot-time importer is react-native-gesture-handler's guarded `require` (`node_modules/react-native-gesture-handler/lib/module/handlers/gestures/reanimatedWrapper.js:5-11`), which catches the constructor throw at `node_modules/react-native-reanimated/lib/module/ReanimatedModule/NativeReanimated.js:46-47`; no app code imports `react-native-reanimated`, `ReanimatedSwipeable` or `react-native-screens/reanimated|gesture-handler` (0 hits in source; 0 bundle markers). So the current bundle probably boots on #11/#12. The trap fires on the first unguarded import — the exact shape of `3bac6616`'s `MageBootScreen` — because Metro caches the failed module and re-throws on any later `require`, and tsc/lint/ship-check stay green because the package resolves.
- Failure scenario: any future `import … from 'react-native-reanimated'` (or a library subpath) → `eas update --branch production` → #11/#12 devices (the founder's phone per `82b24e50`) throw at boot → expo-updates rolls back → "no changes no matter how many restarts"; #13/#14 devices run fine → a split fleet nobody can see from the dashboard.
- Fix: (1) purge — remove the `react-native-reanimated` entry from `bun.lock`, `rm -rf node_modules && bun install`, re-run `expo export` and assert the crash string count is 0 (turn that assertion into `scripts/validate-native-surface.ts` wired into `ship-check`: fail if `react-native-reanimated` is resolvable while absent from package.json); (2) stop sharing one runtime across different native topologies — `"runtimeVersion": { "policy": "fingerprint" }` (or bump `version` on every native change). Accept that this makes #11–#14 three runtimes: the fresh build from F1 becomes the only one that matters.
- Effort: M

### F3 — [P1] [CONFIRMED] App Review's own sandbox purchase will unlock the UI while every server-gated AI feature answers 403 and the client tells the reviewer who just bought Pro to "Upgrade to Pro" — the unrecorded App-Review consequence of the known unset RevenueCat webhook secret.
- Where: `curl -X POST https://nteoqhcswappxxjlpvap.supabase.co/functions/v1/revenuecat-webhook` → `Server not configured`, `HTTP 500` (re-verified 2026-09-03; LAUNCH-CHECKLIST §1 saw the same on 08-08); `supabase/functions/_shared/auth.ts:157-161` `status: 403 … error: "This feature requires ${allowed…} or higher. You're currently on ${tier}." code: 'tier_required'`; `utils/aiLimitAlert.ts:91` `'This AI feature is part of Pro. Upgrade to unlock.'`, `:105` `{ text: 'Upgrade to Pro', onPress: () => router.push('/paywall') }`; client tier comes straight from RC entitlements (`contexts/SubscriptionContext.tsx:118-122`) while the server reads `subscriptions.tier`, which only the webhook may write (`LAUNCH-CHECKLIST.md` §1: "a database trigger pins the subscriptions.tier column"). `supabase/functions/revenuecat-webhook/index.ts:151-153` ignores only `TEST` events, so sandbox purchases *will* be honoured once the secret exists.
- Failure scenario: reviewer buys Pro with a sandbox Apple ID → paywall shows Pro → taps AI estimate / Cost X-Ray → 403 → "Upgrade to Pro" alert → rejection 2.1 ("we purchased the subscription; features remained locked").
- Fix: set `REVENUECAT_WEBHOOK_SECRET` and `REVENUECAT_SECRET_API_KEY`, register the webhook URL in RevenueCat, then prove it with one sandbox purchase on a TestFlight build and a `select tier from subscriptions where user_id = …` before submitting.
- Effort: S (owner configuration)

### F4 — [P1] [CONFIRMED] The privacy policy and the planned App Privacy labels omit data the binary demonstrably collects — precise location, microphone audio, and Sentry crash/performance/session-replay — and the policy never names Sentry.
- Where: `https://mageid.app/privacy` (fetched 2026-09-03, 8,572 bytes): 0 occurrences of `sentry`, `crash`, `replay`, `location`, `gps`, `microphone`, `audio`; processors named are Supabase, Stripe, RevenueCat, PostHog, Resend, Apple/Google, Gemini. Binary: `app/_layout.tsx:132-164` `Sentry.init({ … tracesSampleRate: 0.1, replaysSessionSampleRate: 0.1, replaysOnErrorSampleRate: 1, integrations: [Sentry.mobileReplayIntegration(), Sentry.feedbackIntegration()] })`; location reads at `utils/photoGeoStamp.ts:77-86`, `components/TakeoffFieldVerifyButton.tsx:94-96`, `utils/location.ts:49-56`, `app/post-rfp.tsx:237`; microphone at `components/VoiceCaptureModal.tsx:134-144` (audio uploaded to the STT edge function). `docs/app-store-metadata.md:127-140` label table has no Location, Audio Data, Device ID, Phone Number, Physical Address, Performance Data or Other Diagnostic Data rows.
- Failure scenario: Apple's binary scan sees CoreLocation/AVAudio plus the purpose strings; labels declare no Location/Audio → 5.1.1(i)/5.1.2 metadata rejection, or a post-launch "app privacy details are inaccurate" notice; separately, masked screen replays go to a processor the policy does not disclose.
- Fix: declare (all "linked to user", "used for tracking: No"): Location → Precise Location; User Content → Audio Data, Photos or Videos, Customer Support (feedback widget), Other User Content; Contact Info → Name, Email, Phone Number, Physical Address (project addresses); Identifiers → User ID, Device ID (`utils/posthog.ts:26` anon id, RC app-user id); Diagnostics → Crash Data, Performance Data, Other Diagnostic Data (replay); Usage Data → Product Interaction; Purchases → Purchase History. Add a Sentry paragraph (crash, performance traces, masked session replay, `sendDefaultPii: false`) and GPS/voice paragraphs to the policy.
- Effort: S

### F5 — [P2] [CONFIRMED] Production crash reports are unsymbolicated by construction: source maps and dSYMs are never uploaded, no OTA source-map upload exists anywhere, and the only record of the decision is a commit body.
- Where: `eas.json` production env `"SENTRY_DISABLE_AUTO_UPLOAD": "true"`; commit `998f8aef` body — "Auth token is required… SENTRY_DISABLE_AUTO_UPLOAD=true skips the upload… To re-enable symbolicated crash reports later: add SENTRY_AUTH_TOKEN as an EAS secret and remove this flag"; `eas env:list --environment production` → "No variables found"; grep for `sentry-expo-upload-sourcemaps|sentry-cli|SENTRY_AUTH_TOKEN` across package.json, docs, LAUNCH-CHECKLIST, DEPLOY-VERIFIED → nothing; `metro.config.js` uses `getSentryExpoConfig` (debug IDs are emitted, so uploaded maps would match). The flag also disables the "Upload Debug Symbols to Sentry" phase, so native crashes are blind too.
- Failure scenario: first production crash → Sentry shows Hermes bytecode offsets inside a 21 MB bundle → no line to fix during the launch window.
- Fix: add `SENTRY_AUTH_TOKEN` as an EAS secret and delete the flag; append `npx sentry-expo-upload-sourcemaps dist` to the OTA recipe in DEPLOY-VERIFIED (run after every `eas update`).
- Effort: S

### F6 — [P2] [CONFIRMED] No universal links: `associatedDomains` is absent and the AASA path is swallowed by the SPA fallback, so every https link the app emails (invites, share links, portal) opens Safari, never the app; the native auth redirects rely on custom-scheme 302s.
- Where: introspected config → `associatedDomains: None`, entitlements only `aps-environment` + `com.apple.developer.applesignin`; `netlify.toml` `[[redirects]] from = "/*" to = "/index.html" status = 200`; `curl https://app.mageid.app/.well-known/apple-app-site-association` → `HTTP 200 type=text/html size=1213` (index.html); `public/.well-known/` holds only `manifest.webmanifest` and `robots.txt`; native flows use the scheme instead — `contexts/AuthContext.tsx:520-522` (`emailRedirectTo … : PRIMARY_SCHEME`), `:717-719` (reset), `:791` (`makeRedirectUri` for magic link); `eas.json` `"EXPO_NO_CAPABILITY_SYNC": "1"` means the Associated Domains capability must be toggled by hand when added.
- Failure scenario: a sub or homeowner taps an invite on iPhone → Safari → web login, not the installed app; magic-link/reset from Gmail's in-app browser (which blocks custom-scheme redirects) dead-ends.
- Fix: `ios.associatedDomains: ["applinks:app.mageid.app"]`; add `public/.well-known/apple-app-site-association` (appID `HKT2J284D2.com.mageid.app`, paths for `accept-invite`, `shared-*`, `client-view`, `prequal-form`, `reset-password`, `claim-crew`) plus a `[[headers]]` entry for `Content-Type: application/json`; enable the capability on the App ID; native build.
- Effort: M

### F7 — [P2] [CONFIRMED] The prepared App Store description promises a marketplace where contractors "bid" while `RFP_BROWSE_ENABLED = false` hides the feed — 2.3.1 accurate-metadata exposure on the very feature that was gated off for review.
- Where: `docs/app-store-metadata.md` description — "Optional marketplace — homeowners post projects, you bid. An inbound channel you're never forced to pay for."; keywords include `bid`; `app/(tabs)/mage-id-bids/index.tsx` `RFP_BROWSE_ENABLED = false` (founder decision #1 in START-HERE).
- Failure scenario: reviewer reads the description, opens Bids, finds no way to browse or bid → 2.3.1 "metadata does not match the app".
- Fix: reword for 1.0 ("homeowners post projects; contractor bidding is rolling out") or drop the line and the `bid` keyword until the 1.2 kit ships.
- Effort: S

### F8 — [P2] [CONFIRMED] No App Review path is prepared: login is mandatory, the only data-rich account is the owner's, and the owner override forces the `business` tier and exposes the dev seeders — a reviewer handed that account cannot exercise IAP and sees "Demo Seeder" / "Flagship demo" rows.
- Where: `contexts/SubscriptionContext.tsx:238-247` `if (isOwner(user?.email)) { … resolved = 'business'; }` and `:255-262` (listener re-applies it); `app/(tabs)/settings/index.tsx:1287-1313` owner-only rows "Flagship demo (Overlook Estate)" and "Demo Seeder"; `app/dev-seeder.tsx:85,1105` / `app/dev-flagship-seeder.tsx:83,1312` correctly `Redirect` non-owners; `app/_layout.tsx:366-378` unauthenticated → `/login`; `docs/app-store-metadata.md` has no "App Review Information" section (its "demo script" is a marketing video). A non-owner account *can* get data: `components/DemoSeedPickerModal.tsx` ("Sample projects are local-only"), `app/(tabs)/(home)/index.tsx:953`.
- Failure scenario: reviewer notes say "use omirmajeed2000@gmail.com" → paywall reports Business already active → "unable to locate the in-app purchase" (2.1) or questions about demo tooling (2.2).
- Fix: create a non-owner reviewer account with email + password (note in the review notes that password entry is behind "Sign in with password instead", `app/login.tsx:421`), load a sample project, write App Review notes: persona selection, that Stripe Connect / invoice pay links are for real-world contractor services (3.1.3(e)), where the paywall lives, that IAP products are attached to the version.
- Effort: S

### F9 — [P3] [CONFIRMED] Boilerplate "Always" location purpose strings ship in the binary although the app only ever requests when-in-use.
- Where: introspected Info.plist and `ios/MAGEID/Info.plist` — `NSLocationAlwaysAndWhenInUseUsageDescription` / `NSLocationAlwaysUsageDescription` = `"Allow $(PRODUCT_NAME) to access your location"` (expo-location plugin defaults; `app.json` has no `expo-location` plugin entry); all four call sites use `requestForegroundPermissionsAsync` only.
- Failure scenario: a static-review flag under 5.1.1 purpose-string guidance; harmless at runtime.
- Fix: `["expo-location", { "locationWhenInUsePermission": "<current string>", "locationAlwaysAndWhenInUsePermission": false, "locationAlwaysPermission": false, "isIosBackgroundLocationEnabled": false }]` in `plugins` (needs a native build, which F1 already requires).
- Effort: S

### F10 — [P3] [CONFIRMED] Dead quick-action config: the `ios.infoPlist.UIApplicationShortcutItems` block (3 items, incl. "New jobsite photo") is overwritten by the `expo-quick-actions` plugin's 4 items; the photo shortcut never exists.
- Where: `app.json` infoPlist block (`com.mageid.app.shortcut.new-photo|daily-report|quick-estimate`, added `28fcda4d` May 14) vs the plugin block (`daily-report`, `estimate`, `waiting-on`, `ask`, added `8271b50e` Jul 30); introspected and prebuilt Info.plist contain only the plugin's four; `app/_layout.tsx:363` `useQuickActionRouting()` routes on `params.href`, which the infoPlist items lack anyway.
- Fix: delete the infoPlist block, or add the photo action to the plugin list with an `href`.
- Effort: S

### F11 — [P3] [CONFIRMED] Doc drift that will misdirect release day: CLAUDE.md names EAS project `9f6536e0-…` while every artifact uses `00860b05-b25b-4b37-b85a-846ecb80bb4c`; `delete-account` says no analytics tool exists while PostHog is live; the paywall/legal copy claims "Cancel anytime in the App Store" correctly but PRODUCT-BIBLE §13 still says keys are `buildwise_*`.
- Where: `CLAUDE.md` "EAS project `9f6536e0-…`" vs `app.json` `extra.eas.projectId` / `updates.url` / `ios/MAGEID/Supporting/Expo.plist` (renamed in `2206cd24` "fresh project"); `supabase/functions/delete-account/index.ts:25-26` "Anonymized aggregate analytics. None collected; … no event logging tool" vs `utils/posthog.ts:20` (hard-coded `phc_…` key, always on) and `app/_layout.tsx` `AnalyticsManager`.
- Fix: correct the three lines.
- Effort: S

### F12 — [P3] [CONFIRMED] Release-plumbing leftovers: `EXPO_NO_CAPABILITY_SYNC=1` (from the 2026-04-16 scaffold commit `770ca270`) disables EAS capability sync; the `development` profile sets `developmentClient: true` with no `expo-dev-client` installed; build #14's artifact expires 2026-09-22; `react-native-maps` 1.27.2 sits in `node_modules` with no package.json entry.
- Where: `eas.json` production env; `eas build:list` → #14 `expirationDate 2026-09-22T02:07:57Z`; `node_modules/react-native-maps/package.json` (Apr 21) and the introspect plugin history listing `react-native-maps`; `package.json` has neither.
- Failure scenario: adding Associated Domains (F6) silently fails to sign until the capability is toggled by hand; `eas build --profile development` errors; `eas submit --latest` after 09-22 has nothing to submit (moot once F1 produces #15).
- Fix: drop the env var after confirming capabilities in the developer portal; add `expo-dev-client` or delete the profile; the node_modules purge in F2 removes maps too.
- Effort: S

### Ranked submission-risk matrix (likelihood of rejection × effort)

| # | Risk | Rejection likelihood if unaddressed | Effort | Finding |
|---|---|---|---|---|
| 1 | Submitting an existing build (#11–#14): embedded JS still sells $19/$49 client subs outside IAP and shows an un-moderated RFP feed | Near-certain (3.1.1, 1.2, 2.3.1) | S — one build from HEAD | F1 |
| 2 | Reviewer's sandbox purchase → 403 → "Upgrade to Pro" | High (2.1) | S — two secrets + webhook URL | F3 |
| 3 | Privacy labels/policy omit Location, Audio, Sentry | Medium-high (5.1.1(i), 5.1.2) | S — ASC form + policy page | F4 |
| 4 | Reviewer account is the owner account (tier override, dev rows) | Medium (2.1/2.2) | S | F8 |
| 5 | Description promises contractor bidding | Medium (2.3.1) | S | F7 |
| 6 | IAP products not attached / offerings fail → paywall says "In-app purchasing is currently unavailable" (`app/paywall.tsx:490-495`) | Medium (2.1) | S — ASC config | checklist |
| 7 | Generic Always-location strings | Low (5.1.1) | S | F9 |
| 8 | Reanimated trap re-armed (post-launch OTA rollback, not a review issue) | n/a — release-recoverability | M | F2 |
| 9 | Unsymbolicated crashes (post-launch blindness) | n/a | S | F5 |
| 10 | No universal links (product, not review) | n/a | M | F6 |

### Release-day checklist gaps (not in LAUNCH-CHECKLIST.md or DEPLOY-VERIFIED)

1. **Native build #15 from HEAD before `eas submit`** — the only step that puts the Sep-2 fixes in the reviewer's first launch (F1). Then `eas update --branch production` for the four TestFlight builds.
2. **RC webhook secret + URL, proven with one sandbox purchase** on the TestFlight build, checked against `subscriptions.tier` (F3).
3. **App Store Connect:** IAP products `com.mageid.{pro,business,enterprise}.{monthly,annual}` attached to version 1.0.0 and "Ready to Submit"; Paid Applications Agreement accepted (PRODUCT-BIBLE §12); App Privacy labels per F4; Privacy Policy URL + Support URL live (`mageid.app/privacy` and `/terms` return 200 — verified); EULA choice (standard Apple EULA is fine; the paywall links Terms at `app/paywall.tsx:270-274, 591-596`); Phased Release toggled on; check for a lingering **1.1.0** version record from the Jul-16 bump/revert (`2a83d3b2` → `6c89c830`) that would block a 1.0.0 submission.
4. **App Review Information:** non-owner demo account + notes (F8); mention Sign in with Apple is offered first (`app/login.tsx:299-316`).
5. **Sentry:** `SENTRY_AUTH_TOKEN` secret, remove the flag, OTA source-map upload step (F5).
6. **Node_modules/lockfile purge + export assertion** before any `eas update` (F2); decide fingerprint policy.
7. **Apple credentials nobody can see from the repo:** APNs key uploaded to EAS (`eas credentials`), the App ID has Push + Sign in with Apple (implied by the successful builds), Apple private-email-relay domain registered for Resend so Hide-My-Email users receive invites/magic links, Supabase Auth redirect allow-list contains `mageid://**` (F6).
8. **On-device pass of the five checks START-HERE lists**, plus: fresh install with airplane mode (boot gate at `app/_layout.tsx:545-551` resolves via AsyncStorage fallbacks in `contexts/ProjectContext.tsx:1640-1655`, but Supabase fetches have no timeout on captive networks), deny every permission once, delete an account created via Sign in with Apple.
9. **Rollback runbook:** `eas update --branch production --republish --group <previous>` and `eas update --branch production --rollback-to-embedded` are available (`eas update:view` exposes `isRollBackToEmbedded`), but nothing documents which group is "known good"; record the group id of every production publish in DEPLOY-VERIFIED.

## ADD / CONNECT / DO BETTER (ranked by leverage)

### O1 — Fingerprint-based runtime + a native-surface guard in ship-check — leverage: GCs don't update apps; one bad OTA on a jobsite phone is a week of "the app is broken" and expo-updates' silent rollback hides it from the dashboard — evidence of the gap: `app.json` `runtimeVersion.policy: appVersion`; EAS fingerprints `fb51c1a8` / `ba6d0a07` / `38b22d6f` under one runtime; `bun.lock:1840`; exported bundle contains `Native part of Reanimated` — sketch: `policy: "fingerprint"`; `scripts/validate-native-surface.ts` = `expo export` to a temp dir + assert forbidden strings absent + diff `package.json` native deps against `node_modules`; wire into `ship-check` (the guard-coverage validator will enforce it).
### O2 — Staged OTA rollouts + a recorded known-good group — leverage: limits blast radius of the "audit sweep" mega-update DEPLOY-VERIFIED step 5 wants to ship in one go — evidence: no `eas channel:rollout` usage or documentation; `eas update:view` shows `isRollBackToEmbedded` unused — sketch: publish, `eas channel:rollout production --percent 10`, watch Sentry (needs F5), then 100%; write the group id into DEPLOY-VERIFIED.
### O3 — Universal links for the growth loop — leverage: the portal/sub/prequal links are how homeowners and subs meet MAGE; today every one opens Safari on iOS — evidence: F6 — sketch: AASA in `public/.well-known` + `associatedDomains` + `+native-intent.tsx` already routes any in-app path.
### O4 — App Review kit as a repo doc — leverage: first-submission pass rate — evidence: `docs/app-store-metadata.md` has listing copy but no reviewer notes, no demo account, no IAP attach step — sketch: add "App Review Information" section (F8) and a pre-submit checklist that includes F1's "build from HEAD".
### O5 — Symbolicated releases tagged per OTA — leverage: on-call visibility during launch — evidence: F5; `Sentry.init` sets no `dist`/`release` — sketch: `SENTRY_AUTH_TOKEN`, remove the flag, `dist: Updates.updateId ?? Application.nativeBuildVersion` so an OTA's crashes are separable from the embedded bundle's.
### O6 — Make the paywall tell the truth when StoreKit is unreachable — leverage: reviewers and offline GCs both hit it — evidence: `app/paywall.tsx:190-196, 490-495` shows list prices with "In-app purchasing is currently unavailable" — sketch: hide the three purchase CTAs when `isFallbackPricing`, keep Restore, and surface a retry.

## Appendix — lower-severity notes (one line each with file:line)

- Verified OK — privacy manifest: `ios/MAGEID/PrivacyInfo.xcprivacy` (prebuild template + RN aggregation `ios/Podfile:54` `privacy_file_aggregation_enabled`) declares FileTimestamp C617.1/0A2A.1/3B52.1, UserDefaults CA92.1/C56D.1, DiskSpace E174.1/85F4.1, SystemBootTime 35F9.1; SDK manifests present for RevenueCat, Sentry, GoogleSignIn, AppAuth, React-Core (`node_modules/react-native/React/Resources/PrivacyInfo.xcprivacy`); `NSPrivacyTracking false`; no `ios.privacyManifests` needed — ITMS-91053 does not apply. (`NSPrivacyCollectedDataTypes` is empty; optional, harmless.)
- Verified OK — Sign in with Apple parity (4.8): native `AppleAuthentication.signInAsync` with hashed nonce → `signInWithIdToken` (`contexts/AuthContext.tsx:963-1012`), listed above Google on both screens (`app/login.tsx:299-336`, `app/signup.tsx:186-223`), entitlement present, `usesAppleSignIn: true`.
- Verified OK — account deletion 5.1.1(v): Settings → "Delete Account" → typed DELETE → `delete-account` edge fn → local sign-out + cache wipe (`app/(tabs)/settings/index.tsx:401-466`, `contexts/AuthContext.tsx:661-699`); unproven on device for INVITED accounts (START-HERE) and `delete-account` is on the undeployed list (`DEPLOY-VERIFIED-2026-09-02.md` step 4) — the applied FK cascades cover the old copy.
- Verified OK — no Stripe purchase of digital goods on iOS: `create-rfp-checkout` has zero client callers; `components/ClientPaywall.tsx:82,107` both kill switches off; `hooks/useClientPaywall.ts:95-97` never shows the modal; remaining Stripe surfaces are Connect onboarding and invoice pay links for real-world contractor services (3.1.3(e)).
- Verified OK — paywall 3.1.2 disclosures: Restore (`app/paywall.tsx:587`), price + "per month" (`:380-385`), Privacy/Terms links (`:583-592`), auto-renew text (`:596`), Manage Subscription deep link (`app/(tabs)/settings/index.tsx:1506`); trial badge only from a web-persisted intent (`app/_layout.tsx:1410-1412`), never on native.
- Verified OK — notification prompt timing: `registerForPushNotifications()` defaults `prompt: false` (`utils/notifications.ts:28-46`); home-card nudges arm without `prompt` (`components/home/MorningBriefCard.tsx:81`, `WeekCloseCard.tsx:135`); prompts only from Settings toggles.
- Verified OK — export compliance `ITSAppUsesNonExemptEncryption: false`; no `UIBackgroundModes`; `aps-environment` auto-added by expo-notifications; ATS `NSAllowsArbitraryLoads false`; icon 1024² no alpha; no contacts/calendar/Bluetooth/ATT APIs (`expo-contacts` absent; `NSPrivacyTracking false`).
- Verified OK — dev surfaces: `dev-seeder`/`dev-flagship-seeder` owner-gated; `LogBox.ignoreAllLogs()` and the sidebar debug block are `__DEV__`-only (`app/_layout.tsx:60`, `components/DesktopSidebar.tsx:439`); the only baked keys are public-by-design (RC `appl_`, Supabase anon, PostHog `phc_`, Sentry DSN, Google client ids); the `rcb_sb_` web key is the known sandbox.
- Verified OK — first-launch: fonts are bundled `@expo-google-fonts` with a 1.2 s splash failsafe (`app/_layout.tsx:1373-1398`); `ErrorBoundary` is theme-independent (`components/ErrorBoundary.tsx:62-70`); `Sentry.wrap` at `:1360`.
- `app/_layout.tsx:141` tags preview builds as `environment: 'production'` in Sentry — preview crashes pollute the production view.
- `components/ClientPaywall.tsx:239` still carries "with a free trial" lede copy for the day the flags flip — rework before enabling.
- `contexts/ProjectContext.tsx:1640-1655` boot queries fall back to AsyncStorage on error but have no fetch timeout — a captive-portal network can hold the crane loader for the fetch's full stall.
- `app.json` `splash` uses the legacy key (works on SDK 54); `expo-av` is deprecated in SDK 54 — plan the `expo-audio` move before SDK 55 forces a native build.
- `eas.json` preview profile has no `autoIncrement` (internal distribution, harmless) and its web key is the sandbox one (known).

## What I could not verify (and how it could be)

- **Which build is on TestFlight / the founder's phone** — `eas build:list` has no submission field; check App Store Connect → TestFlight (expected #14 or #12) — it decides whether F2's mixed-fleet risk is live today.
- **Whether EAS's `bun install` installs the orphan `react-native-reanimated` lockfile entry** (affects only the embedded bundle of the next build, which is guarded anyway) — read the "Install dependencies" log of build #15, or run `bun install --frozen-lockfile` in a scratch clone.
- **APNs key in EAS credentials and Push/SIWA capabilities on the App ID** — `eas credentials --platform ios` (interactive) or the developer portal; the successful STORE builds imply SIWA + aps are present.
- **Supabase Auth redirect allow-list contains `mageid://`** — Dashboard → Authentication → URL Configuration; without it native signup/reset/magic links land on the marketing site.
- **App Store Connect state**: IAP products attached and "Ready to Submit", Paid Apps agreement, a lingering 1.1.0 version, App Privacy answers — ASC only.
- **Apple private-relay email registration** (Sign in with Apple + Resend) — developer portal → Services → Sign in with Apple for Email Communication.
- **On-device behaviour of the first launch, the OTA hand-off, and account deletion via Sign in with Apple** — needs a physical iPhone with build #15 (START-HERE already lists five device checks; add these).
- **The privacy page's full legal adequacy** — I grepped the live page for processor and data-type names only.
