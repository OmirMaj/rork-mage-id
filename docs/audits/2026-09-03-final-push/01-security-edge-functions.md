# Edge-function security — final-push audit — 2026-09-03

## Scope covered (files/paths actually read; commands run)

**Repo (read in full):** every `supabase/functions/*/index.ts` (61 directories: ai, analyze-drawings, analyze-photos, analyze-plan-code, analyze-spec-book, analyze-takeoff, auth-magic-link, award-rfp, claim-crew, coi-expiry-watch, compare-drawings, connect-onboarding, connect-status, construction-answer, convert-pdf-to-images, create-payment-link, create-rfp-checkout, daily-digest, delete-account, fetch-external-data, financing-callback, financing-redirect, geocode-bids, homeowner-weekly-digest, import-schedule, invoice-dunning, mcp, mcp-token, morning-digest, notify, notify-nearby-contractors, og-image, plan-extract, portal-ask-home, portal-link-expiry-notice, portal-mark-viewed, project-invite, project-memory-embed, project-memory-search, public-cost-index, public-lead-intake, qbo-connect-callback, qbo-connect-start, qbo-connect-status, qbo-reconciler, qbo-sync, revenuecat-webhook, safety-detect-hazards, safety-draft-incident, safety-generate-jha, scan-anything, scan-credential, schedule-ical, schedule-ical-url, seal-document, send-email, stripe-webhook, transcribe-audio, unsubscribe, usage-status, validate-portal-passcode, widget-estimate); `supabase/functions/_shared/{auth,verifyUser,cronAuth,mcpToken,urlGuard,embeddings,qbo,email}.ts`; `_shared/qbo-mapping/invoice.ts` (user-scoping only). Client twins: `utils/owner.ts`, `utils/aiRateLimiter.ts`, `utils/aiRateLimiterCore.ts` (LIMITS), `app/paywall.tsx` (AI_LIMITS, FINTECH_PERKS), `utils/stripe.ts`, `app/invoice.tsx` + `app/aia-pay-app.tsx` (payment-link call sites), `app/client-portal-setup.tsx:255` / `app/project-detail.tsx:3886` (portalId minting), `marketing/portal/index.html` (auth headers, URL parsing, `getPortalToken`). `supabase/schema.sql` (guard RPC bodies, `mcp_tokens` policies, counter tables). `DEPLOY-VERIFIED-2026-09-02.md`, `docs/START-HERE.md` banner, both prior audit docs (headings), `docs/deploy/2026-07-07-production-handoff.md:72-74`.

**Production, read-only:** `list_edge_functions` (62 deployed; verify_jwt flags), `get_edge_function` for `notify`, `award-rfp`, `sync-bids`, `fetch-material-price`; `execute_sql` SELECTs on `pg_proc`/`has_function_privilege` (EXECUTE grants for every SECURITY DEFINER function), `cron.job` (secrets redacted), `net._http_response` (last 6 h), `notification_outbox`, `invoices` (dunning markers), `portal_messages`/`portal_budget_proposals`/`sub_submitted_invoices`/`change_order_approvals` recency, `app_config` (key names, value length, JWT role claim only — no secret values printed), `pg_get_functiondef` for `fire_notify`, `portal_mark_item_viewed`, `cost_benchmark_stats`, `public_cost_index`, `gc_for_portal`, `consume_rfp_post_credit`, `submit_sub_change_request`, all `notify_*`/`trg_notify_*` trigger functions and the trigger list. No mutating call was made.

**Cross-checks that passed (not findings):** `MASTER_EMAILS` (`_shared/auth.ts:88-92`) == `OWNER_EMAILS` (`utils/owner.ts:24-28`) == the copy in `mcp/index.ts:98`. `MONTHLY_CAPS` (`_shared/auth.ts:293-367`) matches `app/paywall.tsx:142-150` row for row (drawings 15/50/100, photos 50/150/200, takeoff pages 30/100/300, Cost X-Ray —/—/50/150, Construction Answers —/—/100/300) and `ai_text` = `LIMITS.daily × 30` (`utils/aiRateLimiterCore.ts:119-122`: 5/30/80/150 → 150/900/2400/4500). No numeric mismatch. Stripe signature verification (`stripe-webhook/index.ts:131-184`) is correct (HMAC over `t.rawBody`, constant-time compare, 300 s window). RevenueCat secret compare is constant-time and fails closed when unset (`revenuecat-webhook/index.ts:50-59,128-138`). Vision SSRF guard (`_shared/urlGuard.ts`) is an exact-host allowlist used by all seven image fetchers; `og-image` resolves DNS and re-checks every redirect hop. All 8 pg_cron jobs and the `public_bids_notify_nearby` trigger send `x-cron-secret` from `private.cron_auth`; `verify_cron_secret`, `rate_limit_increment`, `portal_project_for_token`, `grant_rfp_post_credit` have EXECUTE revoked from `anon` and `authenticated`. `ai_usage_*` RPCs are `COALESCE(auth.uid(), p_user_id)` and not anon-executable, so a caller can only burn their own quota.

## Findings (ranked; most severe first)

### F1 — [P1] [CONFIRMED] Three pg_cron targets are deployed `verify_jwt: true` and the cron sends no JWT, so `morning-digest`, `invoice-dunning` and `qbo-reconciler` are rejected by the gateway on every tick and have never run from cron
- Where: production listing — `morning-digest` v9 `verify_jwt:true`, `invoice-dunning` v5 `true`, `qbo-reconciler` v2 `true`; `cron.job` ids 7 (`5 * * * *`), 15 (`0 15 * * *`), 23 (`*/30 * * * *`) send only `Content-Type` + `x-cron-secret`; `supabase/functions/morning-digest/index.ts:414` `const isCron = await isValidCron(req);` (never reached); `supabase/functions/invoice-dunning/index.ts:483`; `supabase/functions/qbo-reconciler/index.ts:120`; `ls supabase/` — no `config.toml`.
- Evidence: `net._http_response` for the last 6 h shows `401 {"code":"UNAUTHORIZED_NO_AUTH_HEADER","message":"Missing authorization header"}` at 18:05, 19:05, 20:05, 21:05, 22:05, 23:05 (job 7) and at every :00/:30 (job 23); the single 200 (`{"success":true,"message":"Data fetch cycle complete"...}`, 22:00) is `fetch-external-data`, whose job (id 4) is the only one that adds an `Authorization` header. `invoices`: 5 rows, 3 overdue candidates, `count(dunning_stage > 0) = 0`, `max(dunning_last_sent_at) = NULL`. `notification_outbox` has no `morning_brief` rows at all.
- Failure scenario: a GC with `digest_enabled` never receives a morning briefing; an overdue client is never reminded (the START-HERE banner's "invoice-dunning is STILL emailing clients a FINAL NOTICE ... on every run" is false — it has never sent one); a QBO-connected GC's invoices stuck in `qbo_sync_status='error'` are never retried and invoices paid in QuickBooks are never marked paid in MAGE. All three fail with no log any human reads (pg_net keeps responses 6 h).
- Fix: redeploy the three with `--no-verify-jwt`, and make the cron jobs survive either flag by adding `'Authorization','Bearer <anon key>'` to jobs 5, 7, 8, 9, 12, 15, 23 (the anon key is a valid JWT; the functions still require `x-cron-secret`). Then F2.
- Effort: S

### F2 — [P1] [CONFIRMED] There is no `supabase/config.toml`, so every redeploy without `--no-verify-jwt` resets a function to `verify_jwt: true`; the documented step-4 deploy command omits the flag and would flip `stripe-webhook`, `mcp`, `notify`, `seal-document` and `homeowner-weekly-digest` — Stripe reconciliation, the Claude MCP connector, trigger/portal notifications and the Friday digest all stop
- Where: `DEPLOY-VERIFIED-2026-09-02.md:205-219` (both deploy commands, no `--no-verify-jwt`); `docs/deploy/2026-07-07-production-handoff.md:72-74` (config.toml "deferred deliberately"); `ls supabase/`.
- Evidence: F1 is this mechanism having already fired — `invoice-dunning` and `homeowner-weekly-digest` were deployed `--no-verify-jwt` on 05-18 (`docs/superpowers/plans/2026-05-18-d2h-unsubscribe-presend-guard.md:288`); `invoice-dunning` (redeployed 2026-07-30), `morning-digest` and `qbo-reconciler` (2026-07-21) are `true` today. Stripe posts with `Stripe-Signature` and no JWT (`stripe-webhook/index.ts:478`); Claude connectors present a `mage_mcp_` token, not a JWT (`mcp/index.ts:61-71`); DB triggers post with no credential (`notify_portal_message_fn`); cron job 8 sends only `x-cron-secret`.
- Failure scenario: founder runs step 4 as written → next Stripe `checkout.session.completed` gets a gateway 401 → Stripe retries for 3 days then gives up → client paid, invoice stays "unpaid", no receipt, RFP post credits never granted. Would be P0 the moment the command runs.
- Fix: commit `supabase/config.toml` with `[functions.<slug>] verify_jwt = false` for the 22 self-authenticating functions (list in the task brief) and `= true` for the rest; add a ship-check that compares it to `list_edge_functions`; edit step 4 to pass `--no-verify-jwt` per function until then.
- Effort: S

### F3 — [P1] [CONFIRMED] Five trigger-driven GC notifications (portal message, budget proposal, CO approval/decline, sub invoice submitted, sub invoice reviewed) are silently dropped: the triggers reach `notify` with no credential, `app_config.notify_key` is empty, and `notify` fails closed on unprivileged callers for those events
- Where: `supabase/functions/notify/index.ts:938-942` (`isPrivileged = isServiceRoleToken(bearer) || isServiceRoleToken(apikey) || (await verifyUser(req)) !== null`), `:266-267` (`if (isAnonCaller && !ANON_ALLOWED_EVENTS.has(event)) return { ok: false, reason: 'event_not_anon_allowed' }`), `:220-226` (the allowlist: contract_signed, selection_chosen, bid_question_asked, bid_question_answered, closeout_binder_sent). Production: `notify_portal_message_fn` posts with `headers := '{"Content-Type": "application/json"}'`; `trg_notify_budget_proposal`, `trg_notify_co_approval`, `trg_notify_sub_invoice`, `trg_notify_sub_invoice_reviewed` call `fire_notify`, which sends `'Authorization','Bearer ' || COALESCE(v_key,'')` with `v_key` read from `app_config.notify_key` — that row has `length(value) = 0` (verified). The deployed `notify` (v19, 2026-06-13) carries the identical check (deployed source lines 938-942).
- Evidence of silence: `notify` answers HTTP 200 `{success:true,result:{ok:false,reason:'event_not_anon_allowed'}}`; pg_net is fire-and-forget; no `notification_outbox` row is written. No `portal_messages`, `portal_budget_proposals`, `sub_submitted_invoices` or `change_order_approvals` rows exist after 2026-06-13, so there is no live sample — the trace is deterministic, not observed.
- Failure scenario: homeowner posts "Can we move the tile install?" in the portal → `portal_post_message` → trigger → notify → dropped. The GC sees nothing on the phone and no email; the client waits.
- Fix: switch `fire_notify` and `notify_portal_message_fn` to the `x-cron-secret` header the `public_bids_notify_nearby_fn` trigger already uses (`(select secret from private.cron_auth limit 1)`) and accept `isValidCron(req)` as privileged in `notify`; or populate `notify_key` with the service-role key (then confirm `app_config` is not readable by anon/authenticated). Add a pg_net non-2xx monitor (O2).
- Effort: S

### F4 — [P1] [CONFIRMED] Any signed-in account (free magic-link signup) is a fully privileged `notify` caller: it can push-notify and email any MAGE user with attacker-authored content branded as the victim's own company, and fan out unlimited email to arbitrary addresses in one call
- Where: `notify/index.ts:938-942` (any GoTrue-verified user → `isPrivileged`), `:277-279` (`gcUserId = payload.gc_user_id ?? payload.contractor_user_id` — attacker-chosen), `:296` `getProfile(gcUserId)` (victim's `push_token`, email, company), `:359-363` `sendPush(spec.pushToken, spec.pushTitle, spec.pushBody, ...)`, `:383-390` email `fromCompanyName: gc.company_name` (the victim's brand), `:758-802` `bidder_recipients` loop with no length cap, `:808` `homeowner_email` arbitrary, `:636`/`:700` `hero_photo_url`/`product_image_url` arbitrary `<img src>`.
- Failure scenario: attacker signs up, POSTs `{event:'co_approval', payload:{gc_user_id:'<victim uuid>', signer_name:'Your client', co_number:'7', co_amount:48000, decision:'approved', change_order_id:'…'}}` → victim's phone shows "CO approved · your project" with a deep link, and an email arrives from "<Victim Co> via MAGE ID". Or `{event:'bid_question_answered', payload:{gc_user_id:self, bidder_recipients:[10,000 addresses], question:'…', answer:'<phishing text + link>'}}` → 10,000 emails from `noreply@mageid.app` in one request (Resend suspension/domain reputation).
- Fix: for user-JWT callers require `gcUserId === caller.id` or ownership of `payload.project_id`; cap every recipient list (≤50) and `payload` size; add `rateLimitCount('notify:user:'+caller.id)` (e.g. 60/h); reserve cross-tenant events (`rfp_awarded`, `nearby_rfp_posted`, `bid_question_answered` fan-out) to the service-role / cron-secret path where the recipients are resolved server-side from `bid_responses`.
- Effort: S–M

### F5 — [P1] [CONFIRMED] Unauthenticated callers can relay unlimited email through `notify`: with any project uuid the anon path resolves a real GC with no rate limit at all, and `bid_question_answered` / `closeout_binder_sent` deliver attacker text to attacker-chosen recipients
- Where: `notify/index.ts:220-226` (both events anon-allowed), `:269` (`if (isAnonCaller && portalId && await exceedsRateLimit(portalId))` — limiter only when a portal id is supplied), `:290-293` (`projects?id=eq.${projectId}&select=user_id` under the service role), `:758-802`, `:818-849`. Project uuids are printed into customer-facing URLs (`invoice-dunning/index.ts:400`, `homeowner-weekly-digest/index.ts:393`, `financing-redirect` `?project=`), and `gc_for_portal(p_portal_id)` is anon-executable (production grant check) so a portal id also yields the owner.
- Failure scenario: `curl -X POST …/functions/v1/notify -d '{"event":"bid_question_answered","payload":{"project_id":"<uuid from any portal email>","bidder_recipients":[…],"question":"…","answer":"…"}}'` with no headers → N emails per call, forever.
- Fix: rate-limit the anon path by resolved `gcUserId` and by client IP regardless of which id was supplied; remove caller-supplied recipient lists from the anon surface (resolve bidders server-side); cap recipients; require the anon `apikey` header at minimum.
- Effort: S

### F6 — [P1] [CONFIRMED] Every portal link in system emails is dead: `invoice-dunning` and the homeowner weekly digest link to `/portal/<project.id>` while the page expects the minted `portal-<id8>-<ts>` id, and every `notify` portal CTA omits the `?t=` access token the page requires
- Where: `supabase/functions/invoice-dunning/index.ts:400` `const portalUrl = \`https://mageid.app/portal/${project.id}\`` (the "View invoice" button, `:255`); `supabase/functions/homeowner-weekly-digest/index.ts:392-394` (same, "View your portal"); `supabase/functions/notify/index.ts:304` `portalId ? \`${PORTAL_BASE}/${portalId}\`` and `:814` (used at `:438`, `:688`, `:718`, `:831` "Open my portal" — the homeowner's primary CTA on the closeout binder email); `app/client-portal-setup.tsx:255` and `app/project-detail.tsx:3886` mint `portal-${id.slice(0,8)}-${Date.now().toString(36)}`; `marketing/portal/index.html:4742-4745` `getPortalToken()` reads only `?t=`; `:6884-6888` `fetchSnapshotFromServer` returns null without a token → `showFallback()`; `portal_get_snapshot_v2` raises `portal_denied` without the token.
- Failure scenario: client receives "Final notice — Invoice #12 is 15 days overdue … View invoice" → the fallback page, no invoice, no pay button. Homeowner receives "Your closeout binder is ready … Open my portal" → fallback. (Dunning is not running today — F1 — so this bites the day F1 is fixed.)
- Fix: one `_shared/portalLinks.ts` `portalUrlFor(project)` = `https://mageid.app/portal/${client_portal.portalId}?t=${client_portal.accessToken}` (both fields are on the `projects` row these functions already load) and use it in notify, invoice-dunning, homeowner-weekly-digest, portal-link-expiry-notice.
- Effort: S

### F7 — [P2] [CONFIRMED] The `ai` relay's Pro/Business per-feature floor applies only when the client volunteers `feature`; omit it and a free account runs bid leveling, weekly analysis, the estimate wizard, cash-flow forecasting and the budget dashboard prompts
- Where: `supabase/functions/ai/index.ts:125-143`; the comment at `:122` says "Unlisted/absent feature → no floor (free ok)".
- Evidence: `const feature = typeof body.feature === "string" ? body.feature : undefined; const minRank = feature ? FEATURE_MIN_RANK[feature] : undefined; if (minRank !== undefined && …)`.
- Failure scenario: free user replays the estimate-wizard prompt from the bundle without `feature` → full Gemini output, metered only against the free `ai_text` 150/mo.
- Fix: make `feature` required (400 when missing or unknown); the client already sends it for every gated call.
- Effort: S

### F8 — [P2] [CONFIRMED] `create-payment-link` computes the platform fee from client-supplied `userTier`; a GC who sends `userTier:'free'` pays 0 bps instead of 30/50/40 on every invoice, so the fintech take-rate the paywall advertises is client-optional
- Where: `supabase/functions/create-payment-link/index.ts:96-107` (the comment sizes the loss as "$10 saved on a $100k invoice" — 50 bps of $100k is $50, and the loss is per-GC-forever, not per-invoice), `:387-390` `const feeBps = feeBpsForTier(body.userTier)`. The function already performs two service-role reads (`:295`, `:320`) so a `subscriptions` lookup is not the "latency of an auth round-trip" the comment fears. `app/paywall.tsx:128` advertises 0% / 0.5% / 0.4%.
- Fix: derive the tier server-side (`requireTier(req, ['free',...])` from `_shared/auth.ts` gives verified identity + tier in one call and also closes this function's share of F14).
- Effort: S

### F9 — [P2] [CONFIRMED] `send-email` is an authenticated spam/phishing relay: 25 recipients per call, no per-user rate limit, arbitrary HTML and attachments, and the FROM display name is the caller's self-editable `company_name`
- Where: `supabase/functions/send-email/index.ts:52` `MAX_RECIPIENTS_PER_CALL = 25`, `:233-239` (per-call caps only), `:253` `buildFromAddress(callerCompany)` → e.g. "Chase Fraud Department via MAGE ID <noreply@mageid.app>", `:168-176` recipients not validated as email addresses.
- Failure scenario: free account sets `company_name` to a bank's name, loops 25 recipients per call with a credential-harvest HTML body → mageid.app domain reputation and Resend account at risk.
- Fix: per-user hourly bucket via `rateLimitCount('send-email:'+callerSub)` (60 recipients/h free, 500 paid), validate recipients, and put the verified profile email in the FROM/reply-to so the sender is attributable.
- Effort: S

### F10 — [P2] [CONFIRMED] `revenuecat-webhook` downgrades a subscriber to free on CANCELLATION / EXPIRATION / SUBSCRIPTION_PAUSED whenever the RevenueCat REST lookup is unavailable, contrary to its own comment that those events make no change (latent until `REVENUECAT_WEBHOOK_SECRET` is set — a known blocker)
- Where: `supabase/functions/revenuecat-webhook/index.ts:105-114` (`tierFromEvent` returns `'free'` for those types with the comment "We DON'T downgrade here … we simply make no change (handled by the caller for these types)"), `:164-165` `const tier = authoritative ?? tierFromEvent(ev)`, `:175-186` unconditional upsert. `tierFromRevenueCat` returns null when `REVENUECAT_SECRET_API_KEY` is unset or RC answers non-2xx (`:78-84, 99-101`).
- Failure scenario: Pro subscriber turns off auto-renew on day 3 of a paid month (RC emits CANCELLATION immediately; access runs to period end) → `subscriptions.tier = 'free'` that instant → every `requireTier` call 403s for the 27 paid days that remain.
- Fix: for those three event types without an authoritative read, return 200 without writing, or write `end_date = event.expiration_at_ms` and let `lookupTier` expire it.
- Effort: S

### F11 — [P2] [CONFIRMED] `convert-pdf-to-images` caps Enterprise at 50 pages per run — `auth.tier === 'business' ? 200 : 50` — the same strict-equality demotion the vision functions fixed elsewhere
- Where: `supabase/functions/convert-pdf-to-images/index.ts:169-170`; `MONTHLY_CAPS.enterprise.takeoff_pages = 300` (`_shared/auth.ts:355`).
- Failure scenario: an Enterprise GC uploads a 120-page set → `renderPageCount = min(120, 50)` → 50 pages rendered, 70 silently missing from the takeoff, no error.
- Fix: `RANK[auth.tier] >= RANK.business ? 200 : 50`.
- Effort: S

### F12 — [P2] [CONFIRMED] Two functions exist only in production, not in the repo, and are callable by any signed-in user: `sync-bids` writes federal bids into the shared `cached_bids` table under the service role with hard-coded coordinates (39.8, -98.6) and burns the GovCon quota; `fetch-material-price` burns the paid SerpApi quota with no metering
- Where: production listing (`sync-bids` v23 `verify_jwt:true`, `fetch-material-price` v21 `verify_jwt:true`; neither directory exists under `supabase/functions/`). Deployed `sync-bids`: `sa = b.sync_all||false` … `latitude:39.8,longitude:-98.6` … `db.from("cached_bids").upsert(rec,{onConflict:"notice_id"})`. Deployed `fetch-material-price`: no auth beyond the gateway, `searchUrl = "…serpapi.com/search.json?…&api_key=" + SK`, error bodies echo `detail`.
- Failure scenario: any free account POSTs `{"sync_all":true}` → hundreds of bids upserted at the geographic centre of Kansas → every user's "Near Me" feed is polluted (geocode-bids only fills NULL coordinates, so these never get corrected). SerpApi spend is unbounded per account.
- Fix: `supabase functions delete sync-bids fetch-material-price` (nothing in the repo calls them — `grep -rn "sync-bids\|fetch-material-price"` in app code should be confirmed first), and add a ship-check that diffs `list_edge_functions` slugs against `supabase/functions/*`.
- Effort: S

### F13 — [P2] [CONFIRMED] `cost_benchmark_stats` is SECURITY DEFINER, anon-executable, and aggregates every contractor's paid rates with no `public_index_opt_in` predicate; `public-cost-index` promises opt-in only
- Where: production `pg_get_functiondef(cost_benchmark_stats)` — `from public.cost_benchmark_samples where category = lower(p_category) and unit = lower(p_unit) and (p_region = 'US' or region = p_region)`; compare `public_cost_index` — `where s.public_index_opt_in and …`; grant check `anon_exec = true`; `supabase/functions/public-cost-index/index.ts:9-12` states the privacy promise.
- Failure scenario: `POST /rest/v1/rpc/cost_benchmark_stats {"p_category":"framing","p_unit":"sf","p_region":"<small region>"}` with the anon key → p25/median/p75 of five non-opted-in contractors' rates — the product rule "a rate the contractor STATED must never be shown" broken from the public API.
- Fix: add `and s.public_index_opt_in` or `REVOKE EXECUTE ON FUNCTION public.cost_benchmark_stats FROM anon, authenticated` (only edge functions should call it). DB-side, reported here because it is public-API reachable.
- Effort: S

### F14 — [P2] [CONFIRMED] Eight functions identify the caller by decoding the JWT payload with no verification and depend solely on the gateway flag; a bulk `supabase functions deploy --no-verify-jwt` (the obvious "fix" for F1) would let anyone act as any user in seven of them and open the eighth entirely
- Where: `connect-onboarding/index.ts:129-140`, `connect-status/index.ts:83-94`, `create-payment-link/index.ts:268-279`, `create-rfp-checkout/index.ts:46-58`, `project-invite/index.ts:60-74` (and `:273` trusts the unverified `email` claim to accept an invite), `send-email/index.ts:198-209`, `schedule-ical-url/index.ts:57-65` ("no verification needed — Supabase gateway already verified"), `transcribe-audio/index.ts:47-101` (no identity check at all). The deployed `award-rfp` still uses `decodeJwtSub` (repo fixed at `award-rfp/index.ts:81`, not deployed). Twelve sibling functions already use `_shared/verifyUser.ts`.
- Failure scenario (given F1/F2 show the flag is unmanaged): a deploy with the flag flipped → forged `{"sub":"<victim>"}` token → `project-invite accept` grants the attacker a seat on the victim's project; `create-payment-link` mints links against the victim's invoices; `send-email` sends as the victim's company.
- Fix: replace each decode with `await verifyUser(req)` (one import, ~5 lines each); `transcribe-audio` additionally needs a per-user quota.
- Effort: S

### F15 — [P2] [LIKELY] Every per-IP limiter keys on the first `x-forwarded-for` hop, which a client can supply, leaving only the per-target limits (5/h per email for magic links, 30/h per slug for leads, 80/h per contractor for the widget, 10/h per portal for passcodes)
- Where: `auth-magic-link/index.ts:104`, `public-lead-intake/index.ts:134`, `widget-estimate/index.ts:518`, `validate-portal-passcode/index.ts:93`, `portal-ask-home/index.ts:134` — all `(req.headers.get('x-forwarded-for') ?? '').split(',')[0]`.
- Unverified link: whether Supabase's edge proxy overwrites a client-sent XFF or appends to it (standard reverse proxies append, making `[0]` attacker-controlled). Verify with one request carrying `X-Forwarded-For: 1.2.3.4` and a temporary log line.
- Failure scenario: magic-link bomber rotates the header → 5 emails to each of thousands of addresses per hour from `noreply@mageid.app`.
- Fix: take the last hop (or `cf-connecting-ip`/`x-real-ip` when present) and add a global per-endpoint hourly bucket.
- Effort: S

### F16 — [P2] [LIKELY] `import-schedule` parses attacker-supplied workbooks with `xlsx@0.18.5` — the abandoned npm line (CVE-2023-30533 prototype pollution, CVE-2024-22363 ReDoS; fixed only in SheetJS 0.19.3+/0.20.x on the vendor CDN)
- Where: `supabase/functions/import-schedule/index.ts:42` `import * as XLSX from "https://esm.sh/xlsx@0.18.5"`, `:165` `XLSX.read(bytes, { type: "array" })` on up to 8 MB of user bytes.
- Unverified link: whether the edge isolate is reused across other tenants' requests on the same warm worker (Supabase does reuse workers) — prototype pollution would then survive into their requests.
- Fix: `import * as XLSX from "https://cdn.sheetjs.com/xlsx-0.20.3/package/xlsx.mjs"`.
- Effort: S

### F17 — [P2] [CONFIRMED] `financing-redirect`'s emailed `?ref=` link requires an `x-financing-signature` header that an email client cannot send, so every emailed financing link falls through to the marketing site; the `?project=` branch's "token" check accepts any 20-character string
- Where: `supabase/functions/financing-redirect/index.ts:3` ("emailed invoice/estimate link"), `:75-79` `const authed = await validSignature(req, ref); if (!authed) return redirect(FALLBACK_URL);` on a browser GET, `:86-90` `hasToken = (bearer.length >= 20) || (apikey.length >= 20)`.
- Failure scenario: homeowner clicks "Explore financing" in the invoice email → mageid.app home page; referral never reaches `clicked`, partner never receives `partner_ref` (financing is dormant, so latent).
- Fix: sign the ref into the URL (`?ref=<id>&sig=<hmac(ref)>`) and verify the query param; for the portal branch use the portal access token that the page already holds.
- Effort: S

## ADD / CONNECT / DO BETTER (ranked by leverage)

### O1 — Pin `verify_jwt` in `supabase/config.toml` and make every cron self-sufficient — leverage: turns four dead or soon-to-be-dead automations (briefing, dunning, QBO sync, Friday digest) back on and stops the next deploy from killing Stripe reconciliation; a GC's cash-collection loop depends on all four — evidence of the gap: `ls supabase/` (no config), `DEPLOY-VERIFIED-2026-09-02.md:205-219`, `net._http_response` 401s — sketch: commit the 62-entry config (22 `false`, rest `true`); add `scripts/validate-edge-verify-jwt.ts` to ship-check that reads `list_edge_functions` and fails on drift; add `'Authorization','Bearer <anon key>'` to the seven cron jobs; add BetterStack heartbeats to morning-digest, invoice-dunning, qbo-reconciler, coi-expiry-watch, homeowner-weekly-digest (only 3 of 8 crons have one today). ~2 h, closes F1/F2 permanently.

### O2 — Give `notify` a real authorization model and a heartbeat — leverage: this one function is the GC's entire "the client did something" channel; today it is simultaneously deaf to real events (F3) and open to strangers (F4/F5) — evidence: `notify/index.ts:220-226, 266-279, 938-942`, `app_config.notify_key` empty — sketch: (1) triggers → `x-cron-secret` header, `notify` treats `isValidCron` as privileged; (2) user-JWT callers may only address themselves or projects they own; (3) recipient lists capped at 50 and resolved server-side; (4) per-caller and per-resolved-GC hourly buckets on the anon path; (5) a daily SELECT over `net._http_response` for non-2xx and for `result.ok=false` bodies, written to `notification_outbox` as an ops row so failures are visible in-app. ~3 h, closes F3/F4/F5.

### O3 — One `_shared/portalLinks.ts` and a validator — leverage: every customer-facing email CTA (dunning, digest, closeout binder, CO/selection secondary links) currently dead-ends; the portal is the GC's face to the client — evidence: `invoice-dunning/index.ts:400`, `homeowner-weekly-digest/index.ts:393`, `notify/index.ts:304,814`, `marketing/portal/index.html:4742` — sketch: `portalUrlFor({client_portal})` returning `/portal/<portalId>?t=<accessToken>`; extend `scripts/validate-portal-security.ts` to fail on any `mageid.app/portal/` literal in `supabase/functions` that is not built by the helper. ~1 h, closes F6.

### O4 — A single `guardEdge()` for the decode-only functions — leverage: identity, IP and error hygiene stop depending on a dashboard toggle — evidence: F14 sites, F15 sites, error bodies listed in the appendix — sketch: `guardEdge(req, {tier?})` = `verifyUser` (or `requireTier`) + client IP from the last XFF hop + `rateLimitCount` per caller + a sanitized JSON error envelope; migrate the eight functions and `transcribe-audio`. ~3 h, closes F14/F15 and most appendix leaks.

### O5 — Server-authoritative fee tier and Connect requirement in `create-payment-link` — leverage: the fintech take-rate is the paywall's headline monetization line and today it is whatever the client says — evidence: `create-payment-link/index.ts:107, 387-390` — sketch: `requireTier(req, ['free','pro','business','enterprise'])`, use `auth.tier`; reject when `stripeAccountId` is absent in production (platform-mode links put client money in MAGE's balance). ~30 min, closes F8.

### O6 — Production-vs-repo drift guard — leverage: two unaudited functions are live and one poisons shared data — evidence: F12 — sketch: delete `sync-bids` and `fetch-material-price`; ship-check compares deployed slugs to `supabase/functions/*` and deployed `ezbr_sha256`/`updated_at` to the git log of each directory, printing the stale list (the 09-02 deploy doc built this list by hand). ~1 h.

## Appendix — lower-severity notes (one line each with file:line)

- `analyze-photos/index.ts:338-341` drops unfetchable URLs silently and never maps Gemini's `photoIndex` back through `originalIndex` (`:340`), so when one signed URL has expired every punch/RFI item after it is attached to the wrong photo.
- `mcp/index.ts:65-69` accepts the personal token in `?token=` and the trailing path segment — the secret lands in gateway/proxy access logs; prefer header-only and log a hash.
- `mcp-token/index.ts:56-72` no cap on tokens per user (`mcp_tokens` rows unbounded); `mcp/index.ts:49-58` `rest()` returns `[]` on any error, so a DB blip is reported to Claude as "No projects found.".
- `stripe-webhook/index.ts:282-329` invoice credit is a non-atomic read-modify-write deduplicated only by scanning `payments[]` (`:314-319`); no `event.id` table, so two overlapping deliveries can double-credit; `:582` logs 200 chars of `payment_intent.payment_failed` (card/customer PII in logs).
- `revenuecat-webhook/index.ts:147-165` ignores `event.environment` (SANDBOX purchases would grant production tiers) and `app_user_id` is asserted by the client SDK with the public key.
- `_shared/verifyUser.ts:48` compares the presented token to the service-role key with `===` (short-circuit) while `constantTimeEqual` sits at `:70`; `_shared/auth.ts:110-119` the "apikey" sanity check accepts any 20-character string although its comment claims it catches forged JWTs sent without the anon key.
- `_shared/email.ts:303,312-321` unsubscribe/resubscribe "token" is FNV-1a over a hard-coded string in source; `schedule-ical/index.ts:29-30` and `schedule-ical-url/index.ts:17-18` fall back to a hard-coded HMAC secret when `SCHEDULE_ICAL_SECRET` is unset — anyone with repo access can forge both (could not confirm whether the env secret is set).
- `unsubscribe/index.ts:134-141` `GET ?e=<email>` reveals suppression status for any address without a token.
- Production `portal_mark_item_viewed` (SECURITY DEFINER, no token parameter) is executable by `anon` and `authenticated` although only `portal-mark-viewed` (`index.ts:127`, after the access-token check) should call it — anyone with item + project uuids can forge "client viewed" evidence timestamps; `REVOKE EXECUTE … FROM anon, authenticated`.
- Production `submit_sub_change_request(sub_portal_id_in, …)` is anon-executable with no `p_access_token`, unlike every other sub-portal RPC — a sub-portal id alone inserts change requests into a GC's books (DB-domain lead).
- Production `gc_for_portal` / `gc_for_sub_portal` are anon-executable and map any portal id to the owner's auth uuid (feeds F5).
- Error bodies that leak internals to callers: `notify/index.ts:948` `error: String(e)` (PostgREST paths/status), `convert-pdf-to-images/index.ts:458-462` returns `stack`, `award-rfp/index.ts:123` `Award failed: ${text}`, `seal-document/index.ts:97,105,125`, `qbo-sync/index.ts:58`, `qbo-connect-callback/index.ts:113`.
- `transcribe-audio/index.ts:47-101` has no in-code auth and no per-user quota: unlimited 40 MB uploads to the STT vendor from any signed-in account.
- Get-then-increment metering lets N parallel requests at cap-1 all run: `construction-answer/index.ts:543-544,722` (Opus agentic runs, 40k-token budget each), `plan-extract:107-119`, `analyze-plan-code:100-112`, `safety-*`; conversely `ai/index.ts:210` charges before the Gemini call so upstream failures burn quota.
- The paywall's "Advanced AI / day" smart cap (`app/paywall.tsx:144`) is never enforced server-side — `ai_daily_usage_increment` is only called from the client and `ai/index.ts` meters `tier:'smart'` identically to fast.
- `project-invite/index.ts:194` interpolates `projectName` into email HTML unescaped; `:217` `email` is not format-validated.
- `delete-account/index.ts:231-244` deletes `plan-sheets/tmp/*` objects referenced by the caller's own `plan_sheets.image_uri`, which can be pointed at another user's `tmp/` render (acknowledged in-code as accepted).
- `homeowner-weekly-digest/index.ts:506` the GC's "Send preview" button emails the real homeowner invites (the UI copy at `app/client-portal-setup.tsx:1179` admits it) — a preview that reaches the customer.
- `cron.job` id 4 (`fetch-external-data`) has no `timeout_milliseconds`, so pg_net times out at 5 s on every run (`net._http_response` id 9156 `timed_out:true`) while the function completes — a standing false alarm; it also sends the service-role key from `app.settings.service_role_key` in a header the function never checks.
- `utils/aiRateLimiter.ts:9` header comment still says daily caps are 5/75/200; the code (`aiRateLimiterCore.ts:119-122`) says 5/30/80/150 — stale comment only, no numeric drift in enforced values.
- `create-payment-link/index.ts:88-93` platform-owned legacy mode (no `stripeAccountId` → funds land in MAGE's Stripe balance) is reachable by direct API call; the app only omits it when Connect is not enabled (`app/invoice.tsx:514-521`), so the exposure is a GC paying themselves into MAGE's account by mistake.

## What I could not verify (and how it could be)

- Whether Supabase's edge proxy strips or appends a client-supplied `X-Forwarded-For` (F15) — send one request with the header and log `req.headers.get('x-forwarded-for')` in a throwaway function.
- GoTrue's redirect allow-list for `auth-magic-link` `redirectTo` (`index.ts:93,125`) — Dashboard → Authentication → URL Configuration; if the list is loose the magic link is an open redirect.
- Whether `SCHEDULE_ICAL_SECRET`, `REVENUECAT_SECRET_API_KEY`, `FINANCING_CALLBACK_SECRET`, `ANTHROPIC_API_KEY` are set — `supabase secrets list` (names only).
- Isolate reuse across tenants for F16 — Supabase runtime behaviour, not observable from here.
- The 15:00 `invoice-dunning` gateway 401 directly — pg_net retains 6 h of responses; inferred from the identical :05/:00/:30 pattern for the two sibling jobs, the `verify_jwt:true` flag, and zero dunning markers on three overdue invoices.
- Live behaviour of F3/F4/F5 — no mutating request was sent to production; the traces are static end-to-end reads of deployed and repo source plus production configuration.
- Whether `app_config` is readable by `anon`/`authenticated` (matters only if the F3 fix stores a service key there) — `select * from pg_policies where tablename='app_config'`.
